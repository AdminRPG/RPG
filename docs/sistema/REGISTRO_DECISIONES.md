# Registro de decisiones — One Piece: 7 Seas (motor Eternal)

> Acta del **modo guiado**: toda decisión consecuente tomada por el usuario se anota aquí (fecha, decisión, opción elegida, motivo, sistemas afectados). También se registran incoherencias detectadas (con propuesta, **sin** aplicar cambios a reglas) y el mapeo de la BD existente al Anexo A.
>
> Regla: **léeme antes de trabajar** (junto al SUPER AGENTS.md). Si una decisión del usuario rompe la coherencia con un manual, se señala aquí y se documenta la resolución.

---

## F0 — Fundaciones (2026-08-27)

### D0.1 · PLAN-MAESTRO-ONE-PIECE-ETERNAL.md obsoleto
- **Decisión:** **Borrar el archivo** `docs/PLAN-MAESTRO-ONE-PIECE-ETERNAL.md`.
- **Opción elegida:** Borrar (el documento de la era Granblue — Cielos flotantes, Pactos Primordial, Monedas, codename `gbe` — no corresponde a 7 Seas).
- **Motivo:** El usuario prefiere no arrastrar documentación obsoleta; queda en el historial de git.
- **Sistemas afectados:** Documentación del repo. **Nota:** queda prohibido reintroducir `gbe_`, `gbe-`, Granblue, GBF, I-Forge o iforge (regla del repo, se mantiene).

### D0.2 · Ubicación del SUPER AGENTS.md
- **Decisión:** **Raíz del repo — `AGENTS.md` fusionado**.
- **Opción elegida:** Un solo maestro en la raíz que fusiona las instrucciones de código existentes con el sistema de reglas (índice del §3.3 del maestro).
- **Motivo:** Cualquier agente que abra el repo lee primero el maestro completo.
- **Sistemas afectados:** Documentación operativa del proyecto.

### D0.3 · Tablas existentes vs. Anexo A
- **Decisión:** **Recrear las tablas del Anexo A desde cero** como esquema canónico nuevo.
- **Opción elegida:** Se deja de lado el esquema anterior (el esquema de la era anterior, ~55 tablas, JSON pesado, incl. la antigua tabla de tiradas (con dados) con dados) y se crea el esquema del Anexo A como migración única **con prefijo `mybb_ope_*`** (decisión D0.3-bis).
- **Matiz aplicado (no destructivo):** las tablas viejas **no se borran ni se renombran** en F0 — el código anterior (stubs conservados, D0.4) las sigue usando. El esquema nuevo convive en paralelo; la retirada del viejo se decidirá en una fase posterior con el usuario.
- **Motivo:** El manual es la ley (Anexo A.1) y el esquema viejo no se le parece; recrear desde cero evita arrastrar decisiones de la era anterior (incluido el sistema de dados, que el principio 1 prohíbe).

### D0.3-bis · Prefijo de las tablas nuevas
- **Decisión:** **`mybb_ope_*`** (p. ej. `mybb_ope_tramites`, `mybb_ope_personajes`).
- **Motivo:** Sigue la convención MyBB (`mybb_`), evita colisión con las el esquema de la era anterior conservadas y es coherente con la marca `ope_`. El código usará un prefijo configurable.
- **Sistemas afectados:** Todo el esquema nuevo (Anexo A.1).

### D0.4 · Stubs `inc/ope_rol_*.php` (17 archivos)
- **Decisión:** **Conservar como redirección** de compatibilidad.
- **Motivo:** Las páginas y scripts de la era anterior los referencian; se mantienen intactos y se retiran solo cuando su uso desaparezca.
- **Sistemas afectados:** Motor `ope_rol` (compatibilidad).

### D0.5 · Modelo de permisos de la zona staff
- **Decisión:** **Reutilizar la antigua tabla de cuentas** (staff_level 0–3 + flag `staff_narrador` + `staff_rol` por personaje) como base de la capa de permisos, envuelta en helpers nuevos del motor 7 Seas (`ope7_*`).
- **Motivo:** Ya existe, el staff la conoce y evita tablas nuevas duplicadas. Roles: **staff** (rank ≥ 1 o bypass admin), **narrador habilitado** (`staff_narrador=1`), **jugador** (resto).
- **Sistemas afectados:** Seguridad de paneles, `secretos_json` y campos solo-staff (veracidad de rumores, `npc_primario`).

### D0.6 · Alcance de los «seeds de catálogos base» en F0
- **Decisión (aplicada, reversible):** En F0 solo se siembra lo que el motor de trámites necesita: el **catálogo de 67 trámites como datos** (PHP, no BD), la configuración de economía (1 fila) y el registro de skills en el catálogo. Los catálogos de contenido (razas, dotes, islas, frutas, Haki…) se siembran **en su fase** (F1–F5), como indica el mapa de fases del maestro.
- **Motivo:** F1–F5 ya definen sus seeds; sembrar todo en F0 duplicaría trabajo y no aporta al hito de la bandeja.

---

## F1 — Personaje (2026-08-27)

### D1.1 · Puntero de personaje activo (rol/ope)
- **Decisión:** **`personaje_tabla` en la antigua tabla de cuentas** (`ENUM('rol','ope')` junto a `personaje_activo`) + resolver `ope7_pj_activo()`/`ope7_pj_set_activo()`.
- **Opción elegida:** Columna nueva en el puntero existente (migración mínima, datos viejos intactos).
- **Motivo:** `personaje_activo` (int) es ambiguo entre la antigua tabla de personajes (era anterior) y `mybb_ope_personajes` (esquema Anexo A); los personajes nuevos conviven con el ecosistema actual sin romper nada.
- **Sistemas afectados:** Personajes, sesión (`ope_active_pid`), ficha, plugin.

### D1.2 · Orden del wizard de creación
- **Decisión:** **El del maestro**: razas/tribus → atributos (120) → dotes/defectos (balanza 0) → dominios (2 puntos) → rasgos (balanza 0) → técnica inicial → trámite 3.
- **Motivo:** La raza primero porque sus modificadores suman por encima del reparto; cada paso valida sobre lo anterior.

### D1.3 · Siembra de catálogos en F1.0
- **Decisión:** **Cerrados completos + técnicas mínimas**: razas (11), raciales (20), tribus (7), dominios (6+12), especializaciones (24 ramas), dotes (28), defectos (13), rasgos (24) y el catálogo de efectos de técnicas (15) sembrados del manual; las técnicas de personaje entran por trámite 13 (ver D1.4).
- **Motivo:** Los catálogos cerrados son la base del wizard y de la validación; el seed es idempotente por nombre (ids conservados).

### D1.4 · Técnicas iniciales: NO son gratuitas
- **Decisión:** **No existe técnica gratuita al crear** (respuesta libre del usuario: «no, no hay técnica gratuita»).
- **Opción elegida:** La idea de técnica inicial viaja como **nota del trámite 3**; la técnica se construye después por el **trámite 13** pagando PP (T1 = 60 PP).
- **⚠️ Incoherencia señalada (no corregida):** el Manual del Jugador §4.5 dice «empiezas con técnicas tier 2» (Opción B del especialista), lo que sugiere técnicas iniciales al crear. Con esta decisión un personaje nuevo (0 PP) no puede tener técnica hasta tramitar la 13. Documentado; el usuario decide con conocimiento.
- **Sistemas afectados:** Wizard (paso 7), trámites 3 y 13, economía de PP.

### D1.5 · Filtro `get_input` corregido (INPUT_INT = 1)
- **Decisión (aplicada, técnica):** En MyBB, `MyBB::INPUT_INT = 1` (no 2, que es `INPUT_ARRAY`). Se corrigieron los usos de enteros en `crear-personaje.php` y `bandeja.php`.
- **Motivo:** Bug latente: `get_input(x, 2)` sobre un escalar devuelve `array()` → `(int) 0`; la bandeja no habría procesado firmas ni el wizard sus atributos.

### D1.6 · Ligeros automáticos bloqueados → trámite RECHAZADO (no publicado en falso)
- **Decisión (aplicada):** Si la validación de un ligero 100 % automático (4, 45, 50) falla (cronómetro, techo, PP insuficientes, datos incompletos), el trámite **no se publica**: queda `rechazado` con el motivo de la validación y el solicitante lo ve en su hub.
- **Motivo:** Antes el ligero se publicaba igualmente sin aplicar efectos (estado engañoso). La regla de oro se mantiene: la validación decide, la firma no existe en este flujo.

### D1.7 · Estados del ciclo con usuario en el ENUM de `ope_tramites`
- **Decisión (aplicada):** `revision_usuario` y `aceptado_usuario` entran en el ENUM `estado` (ALTER idempotente en `migrate-7seas-esquema.php`) y el anti-duplicado del motor cubre también esos estados (un jugador no abre otro trámite 3/13 mientras el suyo espera su aceptación).
- **Motivo:** Sin ampliar el ENUM, guardar el resultado del ciclo truncaba la columna (Data truncated).

### D1.8 · `ope_temas.tid` AUTO_INCREMENT (el hilo MyBB real se vincula en F4)
- **Decisión (aplicada):** `tid` pasa a AUTO_INCREMENT (sigue siendo PK e identificador de tema; los participantes lo referencian). El thread real del foro se vinculará al registro en F4 (el jugador postea el hilo aparte).
- **Hallazgo técnico (corregido):** En MyBB `insert_id` es **método** (`$db->insert_id()`), no propiedad; `insert_query()` ya devuelve el id. El efecto de apertura de tema usaba `$db->insert_id` (propiedad → null) y creaba participantes con `tema_id = 0`. Corregido.

---

## F2 — Combate (2026-08-27)

### D2.1 · Integración de la Zona B (posteo en dos zonas)
- **Decisión:** **Panel JS bajo el editor MyBB** (hooks `newreply`/`newthread`): un panel compone las cartas del turno en vivo (técnicas de la ficha, consumibles, estados, modificadores), el botón **valida con aviso (nunca bloquea)** y publica la Zona B como bloque bajo el post + persiste `turnos_combate`.
- **Opción elegida:** La recomendada (A) — la experiencia más fiel al manual 11.12 (el rival lee la Zona B y actúa en consecuencia).
- **Sistemas afectados:** Plugin (hooks de posteo), `turnos_combate`, `sala_combate`, editor MyBB.

### D2.2 · Alcance del trámite 62 (muerte) en F2
- **Decisión:** **Núcleo completo + efectos de mundo como esquema**: veredicto del umbral (PV ≤ −(VOL×2) o PE ≤ −RES), banda de calidad (descuidada/digna/leyenda), ficha → reliquia, herencia (PP 60→1.000 · berries 5.000→1M ×0,5/×1/×1,5) y fruta renace con su tabla. Cartel retirado, baja de facción y suceso de ronda quedan **anotados como efectos de esquema** que se aplican cuando F4 exista.
- **Opción elegida:** La recomendada (A).
- **Motivo:** La muerte es el cierre del cap. 11 y su núcleo es transversal (reliquias/herencia); los efectos de mundo dependen de tablas F4 que aún no existen.

### D2.3 · Firma del veredicto de combate
- **Decisión:** **Veredicto independiente firmable en el panel de resolución** (histórico propio por tema); el trámite 2 (cierre de temas) **lo referencia** al computar PP/karma/fama.
- **Opción elegida:** La recomendada (A).
- **Motivo:** Respeto la separación del manual (resolución al cierre, firma staff ↔ IA) sin mezclar el veredicto con la economía del cierre.

### D2.4 · Firmas de resolución en `sala_combate`
- **Decisión (aplicada, reversible):** La firma del veredicto de combate se guarda en columnas propias de `ope_sala_combate` (`resuelto_por`, `resuelto_fecha`, `nota_resolucion`) + `resoluciones_combate` como histórico de bandas (salud del foro).
- **Motivo:** La banda de delta es un dato agregado del foro (se muestra en el panel); el detalle del veredicto queda en `turnos_combate.veredicto` (JSON).

### Hallazgos técnicos F2 (corregidos)
- **Hook de posteo recibe el `PostDataHandler` (objeto), no un array:** `ope7_zonab_on_post` accedía `$post['message']` → fatal al postear el bot (F0) o cualquier hilo. Corregido normalizando objeto/array y resolviendo el personaje por `uid` (activo) en vez de esperar `ope_pid` en el payload.
- **`ope7_resolucion_firmar` no exigía motivo** (solo lo validaba la página): ahora la función lo rechaza directamente (defensa en profundidad; el test lo cubre).

### Cierre F2 (2026-08-27)
- Motor de combate `inc/ope_rol/sistemas/combate.php` (PA, daño, bandas, Tablas 1–3, choque, umbral del dolor, matices, estados I–III, 1vN, sala, resolución de cierre) + seeds cerrados (24 acciones · 34 estados · 10 matices) · trámites 13 y 62 con efectos (técnica → librería + cupo INT/4 + PP por tier; muerte → reliquia + herencia + fruta renace) · herencia reclamable desde el wizard · Zona B (panel bajo el editor, avisos que nunca bloquean, persistencia en `turnos_combate`/`sala_combate`) · panel `resolucion-combate.php` con firma e histórico · enlace desde Zona Staff · tests 66/66 + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK.

---

## F3 — Progresión y economía (2026-08-27)

### D3.1 · Ejecución del calendario on-roll
- **Decisión:** **Avance perezoso en hook** (`global_start`): `ope7_calendario_avanzar()` idempotente por día real — con el primer visitante del día avanza +2 días on-roll (ratio 1=2) y registra el avance en `calendario_foro.avances`. Sin tarea MyBB ni cron externo (InfinityFree no garantiza cron real).
- **Opción elegida:** La recomendada (A).
- **Sistemas afectados:** `calendario_foro`, hook `global_start`, trámite 1 (ancla).

### D3.2 · Alcance de tiendas en F3
- **Decisión:** **Núcleo completo con zona libre**: apertura/cierre/reapertura/reposición/boletín de precios + compra/venta con registro de transacciones y anti-abuso, usando la zona como texto libre (zona por defecto: `mundo abierto`, `zona_id=0`). F4 conecta el catálogo de las 17 islas a `precios_mercado.zona_id`.
- **Opción elegida:** La recomendada (A).
- **Sistemas afectados:** `tiendas`, `tienda_items`, `transacciones`, `precios_mercado`, `carteras`, trámites 15–18.

### D3.3 · Fecha inicial del calendario
- **Decisión:** El mundo **arranca hoy**: `fecha_foro_actual` = fecha real del día de la migración (semilla solo si la tabla está vacía); desde ahí avanza ×2. **Reinicio posible a futuro**: borrar la fila de `calendario_foro` y re-ejecutar la migración (idempotente).
- **Opción elegida:** La recomendada (A), con el matiz del usuario: reiniciable.

### D3.4 · Dónde vive el inventario del jugador
- **Decisión:** **Bloque «Equipo y cartera» dentro de la ficha 7 Seas** (entre Técnicas y Solo-staff): ranuras usadas (equipado `3+FUE/10`, mochila `8+FUE/4`, Tontatta ×2), objetos, almacén, cartera/bóveda. Sin página nueva; el hub `tiendas.php` cubre comprar/vender.
- **Opción elegida:** La recomendada (A), según la estructura ya existente de la ficha.

### D3.5 · Corrección del flujo de reserva al manual (7.3)
- **Hallazgo (corregido):** F1 sumaba el bloque a `reserva` **al comprar** y nunca lo colocaba. El manual dice «al terminar los puntos entran en tu reserva para colocarlos donde quieras». Nuevo flujo: compra → PP + cronómetro (reserva intacta) · al vencer → `ope7_pj_finalizar_entrenamientos` mete el bloque en reserva y cuenta para el nivel (10 → +1 con arrastre) · `ope7_pj_colocar_reserva` los coloca respetando el techo.

### Hallazgos técnicos F3 (corregidos)
- **`puntos_comprados` mal inicializado:** el wizard escribía 120 (presupuesto de creación) en una columna que el manual define como «acumulado desde el último nivel» — el primer entrenamiento completado disparaba saltos de nivel masivos. Ahora los nuevos PJs nacen con 0 (7.1).
- **`ope7_tramite_firmar` publicaba en falso:** los efectos con `BLOQUEADO`/«datos incompletos» se ignoraban al firmar (solo `crear` los honraba para ligeros). Ahora la firma rechaza con el motivo (D1.6 extendido a IA/staff — trámites 6, 15, 18…).
- **Producción (efecto 6) escribía `zona='almacen'`** en `inventario_personaje`, cuyo ENUM no lo incluye (bug F0 latente): ahora escribe en la tabla `almacen` (9.7).
- **Apertura de tienda no descontaba el stock** del almacén del dueño (10.6: «el stock sale del inventario real»): ahora sí.

### F3.6 · Panel staff «Calendario» (Anexo A.3)
- Página `calendario-staff.php` (scope `body.ope-pg-calendario`, card en Zona Staff): fecha on-roll actual con histórico de avances, presentes activos con su ancla y jugadores congelados (y aviso si falta la instantánea), histórico de aperturas/cierres (últimos 20 temas) y **avisos de coherencia** (pasados anclados en el futuro · presentes con duración on-roll > ventana real ×2).
- **Correcciones colaterales:** (a) el efecto 1 **acepta la fecha declarada de los pasados** (7.5: ≤ actual on-roll, futuro → bloqueado) — antes anclaba todo en hoy; (b) **bug F0 latente:** `ope7_tramite_crear` aceptaba el 6º argumento (resultado inicial) pero nunca lo guardaba (`resultado_json => null`) — el wizard y los ligeros perdían los datos; ahora se persiste.
- Verificado por test (bloque [4b]): panel con presente activo + congelado, pasado futuro bloqueado, pasado coherente anclado en su fecha, histórico de cierres. 41/41 + batería completa.

### D4.1–D4.4 · Alcance de F4 (Mundo Vivo)
- **D4.1 Sub-fases:** F4 se entrega por piezas verificables. **F4.1 = semilla del mundo + ronda + panel**; los trámites 19–44 (facciones, bajo mundo, conquista, navegación, barcos) se montan en sub-fases posteriores con su semilla y sus efectos.
- **D4.2 Ronda mensual:** motor de ronda (crear/cerrar, cola de presentes, aplicar lo firmado: islas con motivo, recompensas, precios dentro de banda, periódico en borrador) + **panel staff «Mundo Vivo»** (dashboard, cola de análisis, matriz de islas). La IA propone (skill-mundo-vivo), el staff firma; la visibilidad del periódico es manual (nada se publica solo — 15.2).
- **D4.3 Trámites 19–44:** cada sub-fase lleva su semilla y sus trámites con efecto real. Los que dependen de IA quedan con prompt + firma (el motor ya lo hace).
- **D4.4 Tienda ↔ isla:** la zona de la tienda pasa a ser **su isla del catálogo** (5.14): apertura exige isla + territorio (ubicación del PJ) + Comerciante; `zona_id` = isla; el precio de venta se ancla al mercado **de su zona** (10.4); al cambiar de manos el territorio (conquista, F4.2), `ope7_tiendas_suspender_en_isla()` suspende las tiendas del bando anterior (5.15/16.6). Resuelve el pendiente D3.2.
- **Hallazgo técnico F4.1:** el esquema de `tipos_barcos` tenía `precio` BIGINT para 3 precios (N1–N3); se añade `precios` JSON con el array completo (5.17) manteniendo `precio` = N1. Además, `oraculos_catalogo` y `transportes` no tienen columna `nombre` (clave por `tipo`) — el seed los trata aparte.

### Cierre F3 (2026-08-27)
- Calendario on-roll `sistemas/progresion.php` (semilla = hoy reiniciable, avance perezoso ×2 idempotente en hook `global_start`, histórico de avances) + finalización de entrenamientos y colocación de reserva · **skill-cierre-temas** (fórmula Base×7 factores con bandas cerradas, techo/suelo, redondeo a favor del jugador, banda de tiempo ampliada; el trámite 2 referencia el veredicto de combate D2.3 y libera la congelación `salio_en`) · catálogo `objetos` sembrado (34: armas por calidad, armaduras/escudos, consumibles, herramientas, diales, rarezas) + `economia_config` · ranuras/carga y equipar con cupos Meitou (trámites 6 y 14) · cartera al crear PJ + movimientos cartera/bóveda · **tiendas de jugador** (15–18: apertura con Comerciante/local/capital/bélicos, cierre/reapertura, reposición, boletín con banda 0,5×–2×) + compra/venta con transacciones y anti-abuso · `tienda-general.php` y `tiendas-personales.php` funcionales · bloque «Equipo y cartera» en la ficha · tests 36/36 + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK.

### F4.2-bis · Bloque «Reserva de puntos» en la ficha (7.3)
- La ficha 7 Seas (`ope7_ficha_html`) muestra, para el dueño/staff, un panel **«Reserva de puntos»** entre «Equipo y cartera» y «Solo staff»: puntos pendientes en la cabecera, **stepper por atributo** (botones −/+, input numérico) con el **techo del nivel** (`ope7_pj_techo_atributo`), máximo por atributo (techo − valor actual) y suma live en JS (rojo si supera la reserva; botón deshabilitado si no hay distribución válida). El botón envía `gaccion=reserva` → `ope7_pj_colocar_reserva()` en `ficha.php` (rama 7 Seas, verificado con `verify_post_check`), con flash de éxito/error y redirect. Sin reserva, el bloque muestra el aviso de que los puntos entran al terminar el entrenamiento (7.3).
- Verificado por test (bloque [4c]): bloque con steppers, techo 20 (nv1), máximos por atributo, suma live y form POST · 47/47 + batería completa.

### F4.3 · Cronómetro de dominios (5.3/4.4 + D4.5)
- **Compra de dominio** por el trámite 4 (ligero automático, rama `dominio_id`+`nivel`): adquisición nueva (nv2–nv5, nivel mínimo de PJ 10/20/35/45, cupo INT 1 adicional por 50) o **subida de nivel en nivel** (sin saltos). Coste = base del nivel objetivo × multiplicador anclado (D4.5). Descuenta PP, registra el gasto en `historico_pp` con concepto («Compra de dominio: «X» → nv2 (×1,50)») y arranca el **cronómetro de 15 días** (independiente del de atributos, 4.4; un solo dominio a la vez). Al vencer, `ope7_pj_finalizar_dominios()` (integrado en `ope7_progresion_cron`) sube al nivel objetivo y limpia; notifica al jugador.
- **UI:** bloque de Dominios de la ficha 7 Seas ampliado — cronómetro en curso visible («entrenando → nvN», termina fecha) + formulario de compra/subida para el dueño (select `did:nivel` con el coste y el multiplicador anclado por dominio, `gaccion=dominio` en `ficha.php` con `verify_post_check`, flash + redirect). **Panel staff «Progresión»**: los cronómetros de dominio aparecen en el bloque de cronómetros junto a los de atributos.
- Verificado por test (bloque [4d]): subida de creación ×1,0 = 60 PP, cronómetro → nv2, al vencer sube y limpia, 1.º adicional ×1,5 = 90 PP con cupo INT 1/1, 2.º adicional bloqueado por cupo, gasto −90 con concepto en el libro, panel y ficha muestran el entrenamiento · 58/58 + batería completa.

### F4.3 · Trámite 38 — Navegación/travesía (5.16/17)
- **Módulo `inc/ope_rol/sistemas/navegacion.php`** (trámite 38, naturaleza `ia`, registrado en bootstrap y en el motor):
  - **Validación (17.2):** origen = `personajes.ubicacion_isla_id` (no editable) · un-presente (5.6, la travesía ES el presente) · destino del catálogo 5.14 (≠ origen) · barco de la flota o transporte con pago desde cartera · utensilio declarado debe estar en inventario/almacén.
  - **Límite de mar por barco + madera (18.5):** la madera del casco habilita los mares (Pino→Blue, Roble→+Paraíso, Adán→+NM, Eva→+ZR) y el tipo exige su `madera_minima`. Ruta = tramos por región (Blue→Paraíso = 72+48 h; Blue→NM cruza Paraíso = 3 tramos).
  - **IRT interno (17.3):** base del mar (1–4) + peligrosidad del destino (1–50: 0/+1/+2/+3) + estado del Mundo Vivo (0–2, techo; propuesta staff, dato estructurado pendiente) − mitigadores (Navegante nv1 −1/nv2 −2, Timonel nv3+ −1, Cartógrafo nv4 −1, barco −1 a −3, utensilio −1). Solo-staff, nunca se publica.
  - **Oráculos (17.4):** generación determinista por banda del IRT (0–2 tranquila → 0–1 menor · 3–5 → 1–2 · 6–8 → 2–3 con grave · 9+ → 3+ con daño asegurado); si el staff propone lista, se valida contra el catálogo y la banda. Transporte = ruta segura (máx 1 menor, 17.6).
  - **Tiempo off-roll (17.5):** 72/48/36/36 h por tramo sumables −12 h utensilio −25 % Maestre + horas de incidentes +24 h transporte = plazo real del tema.
  - **Víveres (17.6):** 1 ración/persona/día on-roll +1/+2/+3 por oráculo; se consumen del inventario/almacén al cierre; sin stock → veredicto empeora.
  - **Cierre (17.6):** integrado en el efecto 2 — consume raciones, aplica el daño al barco por grado máximo de los oráculos, `ubicacion = destino` y resuelve la travesía con `incidentes_travesia` logueados.
  - **Vencimiento (17.5):** `ope7_travesias_vencidas()` en el cron — plazo agotado (tema `fecha_real_apertura + tiempo_disponible_h`) → travesía `vencida` y tema cerrado.
- **Seed:** 4 utensilios (Brújula Común, Log Pose, Eternal Pose, Log Pose del NM — 17.7) en `seed-7seas-progresion.php` (34 → 38 objetos, idempotente).
- **Hallazgos:** (a) bug MyBB null→'' en INT (conocido F4.1) reproducido en `travesias` — los opcionales van como 0 en `insert_query`; (b) **incoherencia de número sagrado:** el manual 17.6 dice ración 50 ฿ pero el seed la tiene a 100 ฿ — **anotado, pendiente de consulta** (principio 8); (c) el «+1 si la ruta cruza zona en guerra» del IRT queda como hook (0 por defecto) hasta que el Mundo Vivo tenga el dato estructurado; (d) la matriz 5.14 en el prompt de la skill (17.8) queda como enriquecimiento pendiente (el prompt 38 ya pasa origen/destino/barco/acompañantes/utensilio).
- Verificado por test (bloque [4e], 25 checks): un-presente bloquea · Blue→Blue con Bote de pino OK · IRT 2 → 1 menor · utensilio mitiga −1 IRT y −12 h (IRT 1, 0 oráculos, 60 h) · Pino/Roble bloquean Paraíso/NM · Roble→Skypiea 144 h (media +24) · cierre: 12 raciones consumidas, barco dañado moderado, ubicación = Skypiea · Adán→Dressrosa 120 h (2 oráculos) · vencida por plazo · transporte civil (pago, +24 h, ruta segura) · 83/83 + batería completa verde.

### F4.3 · Facciones — trámites 19–24 (5.12/13) + panel «Facciones» (A.3)
- **Pregunta (D4.7, 13.3/13.4):** dos números/flujo de facciones. **Decisión:** (a) **cupos de cúspide por facción** (recomendados del manual): Emperador del Mar 4, Almirante 3, Director de sección 5, Estado Mayor 5, Señor del bajo mundo 3, Leyenda 5, Cabeza de casa noble 6; Shichibukai 7 (13.2); resto de rangos sin cupo; (b) **`rep_min` duro** en los rangos (requisito que bloquea el ascenso si no se alcanza, no solo sugiere).
- **Migración:** `faccion_personaje` gana `infiltracion_faccion_id`/`infiltracion_rango_id`/`infiltracion_activa` (capa oculta, 13.7/13.8) · seed: `cupo` por rango y `requisitos` con `rep_min` en la cúspide.
- **Módulo `inc/ope_rol/sistemas/facciones.php`** (trámites 20–24, naturaleza `ia`):
  - **20 · Ascenso (13.4):** sin saltos (solo el inmediato siguiente) · requisito duro `rep_min` · **cupo del rango** (espera de cupo si lleno/sobrecupo) · termómetro propuesto por la skill en el mensaje · histórico inmutable (`cambios_faccion`).
  - **21 · Subfacción élite (13.2/13.8):** Shichibukai (cupo 7) concesión/revocación; revocación por romper condiciones → Wanted ×1,5 (5.13). Gorosei es NPC (bloqueado).
  - **22 · Cambio de facción (13.7):** exige baja de la actual, entra por el rango inicial, anti-abuso (un personaje por facción por jugador, 13.7).
  - **23 · Deserción (13.7):** hostil → criminal (Wanted piso 5M + infamia +1, 5.13); pacífica → Aventurero libre.
  - **24 · Infiltración (13.7/13.8):** capa oculta — visible = facción tapadera, real = `infiltracion_*` (staff); revocación restaura la lealtad real. Solo el staff inicia/infiltra.
- **Panel staff «Facciones» (`facciones-staff.php`, scope `body.ope-pg-facciones-staff`, card en Zona Staff)**: `ope7_facciones_panel_html()` — tablero de rangos por facción con cupos y ocupación, cúspide marcada, Shichibukai activos, histórico de cambios inmutable, Wanted por personaje.
- **Hallazgos:** (a) bug MyBB null→'' en INT (conocido) al restaurar `cupo => null` en el test — SQL crudo; (b) **bug real corregido en `ope7_faccion_cupo_libre`**: devolvía −1 tanto para «ilimitado» como para «sobrecupo» (cupo 1 − 2 ocupantes = −1), y el efecto solo bloqueaba `=== 0` — ahora nunca devuelve negativo (lleno/sobrecupo = 0 plazas).
- Verificado por test (bloque [4g], 22 checks): ascenso bloqueado sin rep mínima · espera de cupo con cupo lleno · ascenso OK con termómetro y histórico · cambio Marines→Piratas entra Novato con baja+alta · anti-abuso de duplicado · deserción hostil → criminal (Wanted 5M + infamia) · infiltración con capa oculta y revocación · Shichibukai cupo 7 concesión/duplicado/revocación (Wanted 7,5M) · panel renderiza tablero/élite/histórico sin inline styles · **109/109 + batería completa** (idempotente, 2 corridas).

### F4.3 · Conquista — trámites 34–37 (5.15/cap. 16) + panel «Conquista» (A.3)
- **Módulo `inc/ope_rol/sistemas/conquista.php`** (trámites 34–37, registrado en bootstrap y motor):
  - **34 · Anuncio (16.2/16.3):** valida el **control previo** (salvaje → 0 rondas: declarar + ocupar · guarnición → asedio por fases · territorio de jugador → disputa con defensor invitado), la **presencia justificada** (16.2: `ubicacion_isla_id` o texto que menciona la isla) y el objetivo (isla o zona del catálogo 5.14); **anti-abuso** (no hay conquista activa duplicada sobre la misma isla/zona); calcula las **rondas de asedio requeridas** (16.3: salvaje 0 · nv1–15: 1 · nv16–30: 2 · nv31–45: 3 · nv46–50: 4+; fortificaciones +1) y publica el **suceso público** (hook anuncio → `sucesos` tipo conquista).
  - **35 · Responder al asedio (16.4, ligero):** el defensor declara su defensa activa (guarnición NPC o personaje); pasa a fase `asedio` y crea el **log de asedio por ronda** (`asedios`: acciones, desgaste, veredictos). Sin participación del defensor no hay veredicto.
  - **36 · Resolver/registrar (16.8, staff):** verifica la **duración mínima** (16.3: nunca se resuelve en la ronda del anuncio, salvo salvaje); si gana el atacante: `ope7_isla_actualizar` (afiliación → mixta, fuerza defensiva, quien manda) **con motivo (fuente `conquista`)**, **suspensión de tiendas del anterior dueño** (16.6 → `ope7_tiendas_suspender_en_isla`, el anzuelo D4.4), estado `ganada` + `ganador_id` + `resuelta_ronda`, suceso y periódico/rumores; si gana el defensor: `perdida`.
  - **37 · Reconquista (16.5):** nueva disputa con las mismas cinco fases; exige una **conquista previa** sobre la isla (anti-abuso); sin bonus artificiales para el defensor.
- **Ejércitos y hordas (16.7):** `ope7_conquista_contratar_unidad` (Infantería 10.000/1.000 · Élite 50.000/5.000 · Especialistas 25.000/2.500 — contrato/mantenimiento por ronda; **máx 4 por bando**; más de 2 exige rango alto, D4.8) y `ope7_conquista_contratar_horda` (Mara 10.000 · Masa 50.000 · Marea 200.000; **una sola vez por asedio**; las genera el Mundo Vivo o un bando). **Crones:** `ope7_conquista_mantenimientos()` (sin pago → retirada, 16.7) y `ope7_conquista_abandonos()` (16.5: 2 rondas sin actividad → propuesta; 3.ª → revuelta aplicada con motivo, afiliación → local) integrados en `ope7_progresion_cron`.
- **Panel staff «Conquista» (`conquista-staff.php`, scope `ope-pg-conquista-staff`, card en Zona Staff)**: `ope7_conquista_panel_html()` — conquistas activas por isla (objetivo, fase, rondas requeridas/transcurridas, atacante/defensor, ejércitos con mantenimiento), histórico con motivo (ganada/perdida/abandonada) y aviso del flujo.
- **Migración:** columnas `ganador_id`/`motivo`/`resuelta_ronda`/`ultima_actividad_ronda` en `conquistas` + `conquista_id`/`bando` en `unidades` + `conquista_id`/`contratada_por` en `hordas` (idempotente).
- **Hallazgos:** (a) bug MyBB null→'' en INT (conocido) al insertar `conquistas.zona_id`/`defensor_id` NULL → SQL crudo en el insert; (b) el catálogo tenía 34 como `quien=jugador` + `naturaleza=staff` (incoherente con el flujo del manual: lo inicia el jugador y firma el staff) — se mantiene la naturaleza `staff` (solo el staff firma); (c) el test pilló que tras deserción el PJ queda como cúspide de «Aventurero libre» (único rango → `es_cuspide`) — detalle del seed, no bug.
- **D4.8 (confirmado por el usuario):** «más de 2 unidades exige rango alto o un imperio» (16.7) → cúspide de facción (`es_cuspide`) **o** nivel de personaje ≥ 30 (imperio en ciernes).
- Verificado por test (bloque [4h], 37 checks): rondas requeridas (salvaje 0 · gobierno fd14 → 1) · anuncio con suceso público · anti-abuso de activa duplicada · sin presencia bloquea · defensa activa + log de asedio · unidades (contrato, límite rango alto nv1→bloquea, nv30→OK, máx 4) · horda una vez por asedio · resolución con motivo (afiliación mixta, quien manda, histórico fuente conquista, **tienda suspendida**) · duración mínima (mismo día bloquea) · reconquista con previa / bloqueada sin previa · abandono (1 ronda no propone, 2 propone, 3 aplica + afiliación local) · mantenimiento sin saldo → retiradas · panel sin inline styles · **146/146 + batería completa** (idempotente, 2 corridas).

### F4.3 · Barcos — trámites 39–44 (5.17/cap. 18) + panel «Barcos» (A.3)
- **Módulo `inc/ope_rol/sistemas/barcos.php`** (trámites 39–44, registrado en bootstrap y motor):
  - **39 · Compra/adquisición (ligero):** primer barco **gratis** (bote de remos, 18.4) · compra con precio N1 del tipo + **madera por clase** (18.5: ×0,5 botes/balandros/goletas · ×2 corbetas · ×3 galeones · ×5 acorazados; +25 % mano de obra sin Carpintero/Astillero) · verifica la **madera mínima del tipo** (18.4: `madera_minima` = nombre de la madera del catálogo, no un mar) · **acorazado solo con imperio** (18.4, D4.10).
  - **40 · Construcción (ia):** exige Carpintero con rama Astillero (5.3) + materiales; construye a N1.
  - **41 · Mejora N1→N2→N3 (ia):** un paso a la vez (sin saltos ni retrocesos), coste = diferencia de precio + madera; actualiza la ficha (casco/maniobra/ranuras/plazas).
  - **42 · Módulos instalar/quitar (ia):** ranuras del tipo/nivel (18.6), requisitos de oficio (tienda→Comerciante · resina→Astillero nv4 · kairoseki→Mercado Negro · cocina/lab/enfermería→su oficio · velas→Maquinista Naval), pago del módulo; quitar libera la ranura.
  - **43 · Reparación (ia):** grados leve/moderado/grave (18.7) con Carpintero/Astillero + materiales (madera 5.8 por grado); log en `reparaciones`.
  - **44 · Venta/desguace/baja (ligero):** venta al **50 % del valor de compra** (D4.9) o desguace en materiales (50 % de la madera); el barco sale de flota (`estado = vendido`); `ope7_barco_hundir()` (veredicto) → suceso de mundo «naufragio».
- **Ficha 18.2:** `ope7_barco_crear` calcula casco/maniobra/ranuras/cañones/plazas del tipo×nivel; `ope7_barco_espacio_raza` (18.3: Tontatta 0 · Humana/Skypiean/Mink 1 · Lunarian 2 · Oni/Bucaner 3 · Gigante 5); `ope7_barco_danio_canon` (×30–×150).
- **Panel staff «Barcos» (`barcos-staff.php`, scope `ope-pg-barcos-staff`, card en Zona Staff)**: `ope7_barcos_panel_html()` — flota por jugador con ficha y estados de daño, catálogo de 10 módulos con requisitos, log de reparaciones recientes.
- **Hallazgos:** (a) **bug real heredado de navegación (corregido en ambos)**: `madera_minima` del tipo es el NOMBRE de la madera (18.5), no un mar — `ope7_nav_madera_max_nivel` recibía un nombre de mar y el límite de madera mínima nunca se disparaba; ahora se resuelve contra `maderas_casco`; (b) el ENUM de `barcos.estado` no incluía `vendido` (trámite 44) — migración idempotente lo amplía; (c) bug MyBB null→'' en INT no aplicó aquí (los insert usan `dueno_id` entero, sin NULLs).
- **D4.9 (decidido):** venta de barco = **50 % del valor de compra** (mismo ratio que tiendas NPC); desguace = 50 % en materiales (madera).
- **D4.10 (decidido):** comprar un acorazado (`es_faccion_npc`) exige **cúspide de facción o nivel ≥ 30** (misma regla que D4.8 — «un peón no manda un ejército»).
- Verificado por test (bloque [4i], 29 checks): tipos/maderas sembrados · primer barco gratis (cartera intacta) · balandro 50.000 descontados · carabela con pino bloqueada (madera mínima) / con roble OK · acorazado con nv30 OK (D4.10) · espacio por raza (Mink 1 / Gigante 5 / Tontatta 0) · mejora N1→N2→N3 sin saltos ni retrocesos · módulo tienda sin Comerciante bloqueado / instalado 1/1 / sin ranuras bloquea / retirado · daño leve (450/500) → reparación (activo 100 %) con log · venta al 50 % (25.000) y fuera de flota · panel sin inline styles · **175/175 + batería completa** (idempotente, 2 corridas).

### UX · Hub de trámites del jugador (2026-08-27)
- **D4.11 (decidido por el usuario):** cada trámite con página propia → **56 ficheros `tramite-NN.php`** (wrappers que delegan en el nuevo `inc/ope_rol/tramites/paginas.php`; generador idempotente `scripts/generate-tramite-paginas.php`). Los **11 solo-staff** (18, 21, 24, 30, 36, 49, 54, 55, 59, 60, 61) no tienen página: badge en el hub → bandeja.
- **D4.12 (decidido):** catálogo agrupado en **6 áreas** (Personaje y progreso 1–14 · Economía 15–19 · Mundo Vivo 20–37 · Viaje 38–44 · Poderes 45–51/56–61 · Grupos 52–55/62–67) con tarjetas enlazadas y filtros (Puedo iniciar por defecto · Automáticos · IA + firma · Ver todo).
- **D4.13 (decidido):** al pedir un trámite → **ligeros implementados (1, 4, 14, 17) se ejecutan al instante**; el resto crea la solicitud en la bandeja. El motor ahora **no auto-publica ligeros sin efecto** (marcador `[PENDIENTE]` en el mensaje del efecto → trámite a bandeja), y el guard de solo-staff mira `quien` (el 34 lo inicia el jugador aunque su naturaleza sea `staff`).
- Formularios por trámite con opciones dinámicas por personaje (barco, tienda, dominio, objeto, isla, utensilio filtrados por `data-pj`) y nota «qué pasa al pedir».
- Bug real corregido: `stripos('independiente', 'pendiente')` rompía las compras de dominio (subcadena) → marcador explícito. Verificado: 175/175 + batería completa · `check-inline-styles` limpio · `sync-theme verify` OK.

### F5 · Akumas y Haki (5.18/5.19) — 2026-08-27
- **D5.1 (decidido por el usuario):** semilla con las **6 fichas canon** (19.8, plantilla de 8 bloques completa) + **pool ampliado de 12 frutas T1/T2** (nombres conocidos con ficha básica; la ficha completa se hace bajo demanda con el trámite 49). `scripts/seed-7seas-akumas.php` (idempotente): 18 akumas + 18 objetos «Fruta: X» (categoria `akuma`, mediano 1 ranura, 19.7) + 3 bandas del pool (`akuma_pool_tirada`: nv3+ T1–T2 · nv15+ T3 · nv30+ T4 — T5 nunca por tirada).
- **D5.2 (decidido):** compra con PP (46) → **Familia automática** (asignación del pool, matriz ×1) · **Concepto (×2) y Fruta concreta (×3) a la bandeja** (la IA propone qué fruta encaja, el staff firma el cupo mundial).
- **D5.3 (decidido):** comer la fruta (47) **automático**: consume el objeto, asigna el poder (`akuma_id`), aplica **defectos exigidos y dotes exclusivas** de la ficha en el JSON `datos` con la **balanza a 0** (aviso si no suma 0).
- **D5.4 (decidido):** Conquistador (50) **automático** con RNG real (3/8/15/25/40 % por ventana nv5/15/25/35/45): registra el intento, y al acertar crea el nivel 1 + histórico + **suceso de Mundo Vivo en borrador** (`activo=0`, tipo `haki`) que el staff publica desde el panel.
- **Módulo `inc/ope_rol/sistemas/akumas.php`** (registrado en bootstrap y dispatch 45/46/47/50): pool por nivel (`ope7_akuma_tier_max`), matriz (`ope7_akuma_matriz` 150/300/600/1.000/1.500 ×1/×2/×3), cupo mundial (`ope7_akuma_tiene_fruta`: fruta comida **o reservada sin comer** — el bug de la doble asignación), entrega con objeto+histórico (`ope7_akuma_entregar`), influencia de ficha (`ope7_akuma_influencia`), ventana del Conquistador (`ope7_conquistador_ventana`) y publicación de sucesos (`ope7_akumas_publicar_suceso`).
- **Anti-abuso (19.7.1):** morir para volver a tirar → **sin tirada hasta nv7** (tabla `muertes`). La tirada solo decide qué fruta obtienes; afinidad natural −10 % PE (`personajes.akuma_afinidad`, migración idempotente).
- **Migración:** `objetos.categoria` ENUM + `'akuma'` (idempotente) + `personajes.akuma_afinidad`.
- **Panel staff «Akumas y Haki» (`akumas-staff.php`, scope `ope-pg-akumas-staff`, card en Zona Staff)**: sucesos del Conquistador en borrador con botón Publicar · catálogo y cupos mundiales (quién porta cada fruto) · pool de la tirada · histórico de frutas · vista Haki (niveles/usos/PP + intentos del Conquistador).
- **Ficha 7 Seas:** bloques «Fruta del diablo» (ficha compacta de 8 bloques; «sin comer — trámite 47» si está reservada) y «Haki» (niveles, usos, PP; nota de Armadura/Mantra automáticos a nv10).
- **Formularios 45/46/47/50** en `paginas.php`: tirada y Conquistador con contexto opcional · compra con especificidad/tier/familia · comer con selector dinámico de las frutas propias sin comer (`fuente akumas`).
- **Hallazgos:** (a) MyBB null→'' en columnas JSON (2.º caso conocido tras conquista): omitir la clave (default NULL); (b) `ope7_akuma_objeto_id` usaba LIKE sobre JSON y MySQL normaliza espacios/orden → **JSON_EXTRACT** (inmune); (c) el check de portador debe mirar la fruta **reservada sin comer** (la 2.ª tirada rompía el cupo único `uq_portador`).
- Verificado por test `scripts/test-7seas-f5.php` (46 checks): semilla · tirada nv3 (publicado + cupo + afinidad + inventario + histórico) · 2.ª tirada bloqueada · anti-abuso nv5/nv7 · compra familia T2 (300 PP, libro) · sin saldo bloqueado · concepto/concreta a bandeja sin fruta asignada · comer (consumo + defectos) · duplicados/ajenas bloqueados · Conquistador nv4/nv5/ventanas/agotada · éxito nv45 (nivel 1 + suceso borrador + histórico + publicación) · panel y ficha · **46/46 + batería completa** (idempotente, 2 corridas) · `check-inline-styles` limpio · `sync-theme verify` OK · BD 100 % limpia.
- **Trámite 51 — Subida de nivel de Haki** (5.19/20.x): `ope7_efecto_subida_haki` — valida **tipo despierto** (Armadura/Mantra automáticas a nv10 vía `ope7_haki_auto_despertar`, idempotente y en el cron; Conquistador solo por tirada 50) · **usos acumulados** (1 por tipo y por tema) · **PP** de la escalera (200/300/400/500, se paga entera — adaptabilidad humana no aplica, 20.2) · **VOL efectiva** (base + raza por encima del techo, 20.3: 55/70/85/95 → nv~23/33/42/48). Al publicar: descuenta PP, sube el nivel, registra en `historico_pp` y `haki_historico` (usos, PP, firma, motivo) y la ficha lo muestra. BLOQUEADO → el trámite se rechaza (nunca se publica en falso). **Conteo de usos al cierre** (20.2): el cierre de tema (efecto 2) lee `haki_usos` del veredicto (la IA detecta usos declarados, el staff los confirma al firmar) → `ope7_haki_contar_usos_cierre` suma +1 por tipo despierto y por tema. Prompt específico (case 51) en el catálogo. Formulario 51 (personaje + tipo + contexto) en `paginas.php`. Panel: histórico «Subidas de Haki (51)». Verificado (bloque [5b], 18 checks más): auto-despertar nv10 idempotente · conteo de usos · usos insuficientes → BLOQUEADO y rechazado · N1→N2 completo (6 usos + 200 PP + VOL 55: nivel 2, PP 800→600, libro e histórico) · VOL/PP/tipo-no-despierto/N5 bloqueados · **64/64 + batería completa** (idempotente).

### F5.2 · Narradores y auto-narradas (5.20/cap. 21) — 2026-08-28
- **Módulo `inc/ope_rol/sistemas/misiones.php`** (trámites 52–55, registrado en bootstrap y motor):
  - **52 · Solicitud de auto-narrada (ia):** valida ficha completa de 6 bloques con **condiciones de victoria/fracaso explícitas** (requisito duro, 21.2 — sin ellas se rechaza con motivo), requisitos duros (nivel_min/facción/oficios, 21.2.5), **tasa del tablón** (5.9: descuento de cartera con movimiento) y **un-presente** (5.6). Lanza los **oráculos del acto 1 con el motor de 5.16** (banda del IRT de la isla: nivel de mar + peligrosidad de la matriz 5.14, determinista, sin dados), abre el **tema presente aventura invadible** (`tema_tipo=aventura`), registra al participante y crea el **primer tramo** con el texto de la IA firmado por el staff (`_staff_uid` viaja en el trámite al firmar).
  - **53 · Posteo de tramo (ia):** **anti-abuso 21.3** — sin los posts de la ronda el tramo se BLOQUEA (la IA nunca adelanta un tramo); recoge los posts, calcula el tramo/acto siguiente (`ope7_mision_acto_para`: actos repartidos por duración en rondas), aplica el **oráculo siguiente del plan** si el acto lo pide y crea el tramo con firma; avisa cuando se alcanza el acto final.
  - **54 · Apertura de misión (staff):** publica la ficha de 6 bloques en el tablón (`en_tablon=1`), con validación dura de condiciones/escenas/categoría del catálogo/isla de la matriz; secretos en `secretos_json` (solo staff/narradores).
  - **55 · Cierre de misión (staff):** exige acto final (tramo ≥ duración; abandono anticipado permitido) y motivo escrito; aplica **recompensas con histórico** (berries a cartera · PP al saldo + `historico_pp` · fama a `fama_infamia_expo` · objetos a la mochila), cierra el tema y libera el un-presente, registra la salida de participantes y deja el **suceso de Mundo Vivo en borrador** (5.14, se publica desde el panel).
  - **Cron `ope7_misiones_ronda_cerrar()`** (en `ope7_progresion_cron`): misión en curso cuyo tema se cerró sin cierre de misión → **abandonada con motivo** (5.20, anti-abuso). Sin plazo inventado: el cierre por abandono depende del cierre del tema (números sagrados, principio 8).
- **Panel staff «Narradores y Misiones» (`misiones-staff.php`, scope `body.ope-pg-misiones-staff`, card en Zona Staff)**: tablón CRUD (crear borrador con ficha de 6 bloques, publicar/archivar con validación), **narradores con cupo de 2 simultáneas** (rol `staff_narrador` por personaje en la antigua tabla de personajes + staff por `staff_level`), auto-narradas en curso (tema, tramo/acto, oráculos, botón «Cerrar (55)» que crea el trámite staff), histórico de cierres y **sucesos de misión en borrador con botón Publicar**. Acceso: staff + narradores habilitados (21.5); escritura solo staff.
- **Formularios 52/53** en `paginas.php` (fuentes dyn `misiones` = tablón publicado, `misiones_curso` = misión en curso del PJ) + prompt específico del case 55 en el catálogo (verificación de condiciones + salida JSON de recompensas).
- **Migración:** columnas `tema_id`/`solicitante_id`/`abierta_en`/`oraculos` (JSON con el plan por acto) en `misiones` (idempotente).
- **Hallazgos corregidos (auditoría transversal):** (a) **bug real `invadible` invertido en `ope7_efecto_apertura_tema`** — los presentes se creaban como no invadibles y los pasados como invadibles (DDL y 5.6 dicen lo contrario); corregido, afecta a las misiones (21.4 «tema presente e invadible»); (b) **bug real `my_post_key`** — el form «Publicar suceso» del panel de akumas no enviaba el token y `verify_post_check` fallaba siempre; corregido; (c) **`ope7_es_staff_o_narrador` no reconocía a narradores no-staff** (`is_narrador` exigía `is_staff` — 21.2 dice que el rol es independiente); corregido; (d) `staff_narrador` vive en la antigua tabla de personajes (por personaje activo), no en la antigua tabla de cuentas; (e) bug MyBB null→'' en INT (conocido F4.1) en los inserts de misiones (`isla_id`/`narrador_id`/`salida`/`oraculo_id` → 0).
- Verificado por test `scripts/test-7seas-misiones.php` (34 checks): 54 publica con condiciones y rechaza sin ellas · 52 abre tema aventura invadible + tramo 1 + participante + un-presente · requisitos de nivel bloquean · 53 sin posts bloquea / con posts crea tramo 2 · cierre anticipado bloquea · cierre cumplida aplica berries/PP/fama/objeto, cierra el tema, suceso en borrador y salida del participante · cron abandona con motivo · panel sin inline styles · **34/34 + batería completa verde** (f2 66/66 · f3 42/42 · f4 175/175 · f5 64/64) · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Auditoría de refuerzo (segunda pasada, mismo día):** (a) el motivo del cierre de misión (55) ahora usa `_firma_motivo` (el motivo REAL de la firma viaja al efecto, no el de creación del trámite); (b) join muerto `personajes ON p.id = s.isla_id` eliminado del panel de akumas (los sucesos del Conquistador no muestran personaje — el `isla_id` es la isla, no un PJ); (c) el split de escenas del formulario del tablón limpia los `: ` colgantes tras «Acto N»; (d) `ope7_tramite_es_auto` ahora incluye 45/47/50 (la nota del hub «Se ejecuta al instante» era incorrecta — decía «va a la bandeja» para trámites 100 % automáticos). Re-verificado: batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK.

### F5.3 · Tripulaciones (5.21-ter/cap. 22.9) — 2026-08-28
- **Módulo `inc/ope_rol/sistemas/tripulaciones.php`** (trámites 63–67, registrado en bootstrap y motor):
  - **63 · Fundación (ia, capitán):** mínimo 2 (capitán + 1 fundador escenificando, 22.9), ficha (nombre único `uq_nombre`/bandera/propósito), **barco del capitán con plazas (5.17)** — espacio por raza (Tontatta 0, Gigante 5, vía `ope7_barco_espacio_raza`), un PJ por usuario, barco sin banda previa y **un-presente (5.6)**: crea la entidad + **cofre común (5.9)**, vincula el barco a la banda (`barcos.tripulacion_id`), abre el tema de fundación (presente, `fundacion_tema_id`) y registra el histórico.
  - **64 · Ingreso (ligero/firma, capitán):** solo el capitán (22.3), PJ aprobado sin tripulación activa, espacio del barco y un PJ por usuario; fecha de ingreso.
  - **65 · Baja/expulsión (ligero/firma, capitán):** motivo obligatorio (histórico), libera la plaza y **reparte la parte equitativa del cofre** al saliente (5.9, con registro en el log del cofre y movimiento de cartera).
  - **66 · Cambio de capitán (staff + firma):** cesión o motín con **veredicto (5.10/5.14)** y motivo obligatorio; sucesor debe ser miembro activo; roles intercambiados, el cofre queda en la banda y **suceso de ronda en borrador si cambia el nombre** (5.14).
  - **67 · Disolución (staff + firma):** motivo obligatorio; **reparto equitativo del cofre con residuo al último capitán** (5.9), todos los miembros salen con fecha, **el barco vuelve al último capitán** y queda desvinculado (22.9); estado `disuelta`. **Automática con <2 activos** (22.9): 1ª detección del cron → **aviso con plazo para reclutar** (`aviso_disolucion_en` + histórico); 2ª detección → disolución automática con motivo.
- **Cron `ope7_tripulaciones_ronda_cerrar()`** integrado en `ope7_progresion_cron` (idempotente, tope 50 por pasada).
- **Panel staff «Tripulaciones» (`tripulaciones-staff.php`, scope `body.ope-pg-tripulaciones-staff`, card en Zona Staff)**: avisos de disolución, fichas activas (miembros con espacio, cofre con último movimiento, barco), **acciones del staff** que crean los trámites 66/67 (selector de sucesor por banda en JS, sin estilos inline) e histórico auditable (evento + motivo + firma).
- **Formularios 63–67** en `paginas.php` (fuentes dyn `mis_tripulaciones` = bandas del capitán, `miembros_trip` = miembros activos ajenos, `pj_sin_tripulacion` = PJs aprobados libres) + procesado con `fundadores` múltiples (63).
- **Migración:** columna `aviso_disolucion_en` en `tripulaciones` (idempotente).
- Verificado por test `scripts/test-7seas-tripulaciones.php` (38 checks): 63 bloquea sin fundador/barco ajeno/nombre duplicado/un-presente y funda con cofre + tema + barco de la banda · 64 bloquea no-capitán/duplicado e ingresa con fecha · 65 bloquea sin motivo y reparte el cofre (saliente +1.000 ฿, cofre −1.000) · 66 cesión con cambio de nombre + suceso en borrador, sucesor no miembro bloquea · 67 reparto con residuo al capitán, barco al último capitán, entidad disuelta · cron avisa en 1ª detección y disuelve en la 2ª con motivo · panel sin inline styles · **38/38 + batería completa verde** (f2 66/66 · f3 42/42 · f4 175/175 · f5 64/64 · misiones 34/34) · `check-inline-styles` limpio · `sync-theme verify` OK.

### F5.4 · Cibernética y Familias Legendarias (5.22/cap. 23) — 2026-08-28
- **Módulo `inc/ope_rol/sistemas/cibernetica.php`** (trámites 56–61, registrado en bootstrap y motor):
  - **56 · Instalación de implante (ia):** valida **zona única** (cupo 1 por zona, máx. 3), **puerta de personaje** (nv10/20/35), **requisitos acumulativos** (suma de todos los implantes, 5.22 §A.2 — `int` mapea a la columna `inte`), **balanza a 0** (ranuras + defectos = exactamente 0, 23.2), **vara de Cirujano + Ingeniero** (23.3; autocirugía −1) y **pago berries + PP** (100.000/500.000/2.500.000 ฿ · 200/400/600 PP, con histórico y reversión del cobro si falta PP); aplica los defectos (origen narrativo), registra la modificación + histórico.
  - **57 · Retirada (ligero/firma):** motivo obligatorio; libera cupo y balanza; las mejoras se pierden.
  - **58 · Mantenimiento/reparación (ligero sin firma, se ejecuta al crear):** paga la ronda (2.500/10.000/40.000 ฿; **×2 con «Mantenimiento oneroso»**) y repara el estado averiado.
  - **59 · Diseño de mejora a medida (staff):** la ficha calibrada (ranuras + defectos) viaja en el resultado editable y se aplica al catálogo al firmar.
  - **60 · Concesión de linaje (staff):** cupo mundial (3–5) + un linaje por PJ + motivo (expediente de fidelidad); **crea la dote/defecto del linaje si el catálogo no los tiene** (la dote + «La sangre llama» −1, origen narrativo) + suceso de ronda en borrador (5.14).
  - **61 · Revocación (staff):** motivo obligatorio; retira dote/defecto (origen narrativo), libera cupo, suceso de ronda.
  - **Cron `ope7_implantes_ronda_mantenimiento()`** (en `ope7_progresion_cron`): descuenta la ronda y degrada a **«averiado»** si no hay saldo (23.3).
- **Seed `scripts/seed-7seas-cibernetica.php`** (idempotente): los **3 ejemplos resueltos de la guía 5.22 §8** (Brazo de kairoseki N2 · Ojo mecánico N1 · Exoesqueleto de asedio N3) **recalibrados a balanza exacta 0** (la guía usa «compensa» de forma ilustrativa: +3/−1 o +5/−6 no suman 0; el manual 23.2 manda — el Ojo añade «Mantenimiento oneroso» y el Exoesqueleto puntúa la Ruptura +1) · los **6 defectos de 5.22 §A.6** · las **3 familias** (Línea D. cupo 5 · Vientomar 4 · Cindral 3) con dote/defecto/lore.
- **Paneles staff (A.3, cards en Zona Staff):** `cibernetica-staff.php` (scope `ope-pg-cibernetica-staff`) — implantes activos con zona/nivel/estado/requisitos, **acciones que crean los trámites 56/57/59** e histórico con firma; `familias-staff.php` (scope `ope-pg-familias-staff`) — catálogo con cupos, **acciones que crean los trámites 60/61**, portadores e histórico.
- **Formularios 56–61** en `paginas.php` (fuentes dyn `implantes` = catálogo, `mis_implantes` = activos del PJ, `familias` = con cupo, `pj_sin_linaje`, `linajes_activos`) + prompts 57/58/60/61 en el catálogo (56/59 ya existían). Los 59/60/61 son solo-staff: sin página de jugador (el generador los excluye), se inician desde los paneles.
- **Fix de robustez:** los paneles declaraban solo `global $db` pero usan `$mybb->get_input('my_post_key')` — añadido `$mybb` (el test de tripulaciones no lo detectó porque no llegaba a los formularios).
- Verificado por test `scripts/test-7seas-cibernetica.php` (31 checks): 56 bloquea sin vara / cupo por zona / requisitos acumulados y cobra berries+PP con defecto aplicado · 57 motivo obligatorio y libera cupo · 58 ligero-sin-firma paga 5.000 (2.500 ×2 oneroso) · 59 calibra y restaura el catálogo · 60 motivo/cupo/un-linaje bloquean y aplica dote+defecto+suceso · 61 revoca con motivo · cron degrada a averiado · paneles sin inline styles · **31/31 + batería completa verde** (f2 66/66 · f3 42/42 · f4 175/175 · f5 64/64 · misiones 34/34 · tripulaciones 38/38) · `check-inline-styles` limpio · `sync-theme verify` OK.

### Cierre F6.1 · Bajo mundo (25–33, 5.13) — el último bloque de trámites (2026-08-28)
- **Módulo `inc/ope_rol/sistemas/bajomundo.php`** (registrado en bootstrap + motor, casos 25–33):
  - **25 · Solicitar rumor a la red** (ia+firma): exige red activa propia y espía activo; la **capacidad por tipo** (Novato Suspiro/Murmullo local · Avanzado hasta Susurro regional · Experimentado hasta Alto Susurro · Supremo todas/mundial, 14.2.3) limita categoría y alcance; la IA propone la ficha de 5 campos, el staff fija la veracidad; cobra el mantenimiento de la ronda.
  - **26 · Comprar rumor** (ligero): multiplicadores **fiabilidad ×0,6/×1,0/×1,5 × frescura ×1,2/×1,0/×0,5** con el techo global 0,5×–2× (14.2.2); la ficha queda en poder del comprador (operación); duplicado bloqueado.
  - **27 · Contrastar** (ia+firma): coste por alcance (1.000/5.000/50.000) × sensibilidad (×1–×10); afina la fiabilidad un grado y en **Sólido revela la veracidad interna** (14.4).
  - **28 · Vender** (ligero): transferencia entre jugadores con **copia del vendedor** (14.2.4); sin el rumor en tu poder bloquea.
  - **29 · Montar/ampliar la red** (ligero): contrato (5.000→500.000) + mantenimiento (500→50.000), **límite de 4 espías**, sin saldo bloquea.
  - **30 · Publicar cartel** (staff+firma, acción desde el panel): cifra escala 5.9 (≥100.000), paradero con fiabilidad, **caducidad de paradero a 3 rondas** (14.6) y registro en `recompensas_historico`.
  - **31 · Cobrar recompensa** (ia+firma): entrega verificada (veredicto 5.10), **anti-abuso autocaza**, **paradero frío bloquea** (cron 3 rondas → estado `frio`), cobro + histórico.
  - **32 · Rumor falso** (ia+firma): veracidad interna **falsa** (nunca se reescribe, 14.3), fiabilidad publicada del staff.
  - **33 · Ataque a una red** (ia+firma): **veredicto sin dados** (14.5) — espías descubiertos/retirados y red desactivada según el método declarado.
- **Cron `ope7_bajomundo_cron`** (en `ope7_progresion_cron`): carteles `vigente` con `ronda_caducidad_paradero < ronda activa` → `frio` (14.6, idempotente).
- **Panel staff `bajomundo-staff.php`** (A.3, scope `ope-pg-bajomundo-staff`, card en Zona Staff): rumores por isla con la **veracidad interna marcada solo-staff**, redes y espías (capacidad/mantenimiento, x/4), carteles con caducidad, histórico de `rumor_operaciones` + formulario que crea el trámite 30.
- **Formularios 25–33** en `paginas.php` (fuentes dyn `mis_espias`, `rumores_comprables`, `mis_rumores`, `pj_otros`, `carteles_cobrables`, `redes_ajenas`) + prompts 25/27/30/31/32/33 en el catálogo (26/28/29 ligeros sin prompt). Los ligeros 26/28/29 entran en `ope7_tramite_es_auto`.
- Verificado por test `scripts/test-7seas-bajomundo.php` (**47 checks**): 29 contrato/límite-4/sin-saldo · 25 espía ajeno/capacidad/mantenimiento · 26 precio-techo/duplicado · 27 afina→Sólido-revela · 28 sin-poder/venta+copia · 32 falso · 30 cifra/caducidad/histórico · 31 autocaza/sin-entrega/cobro/frío · 33 propia-bloquea/veredicto · cron · panel sin inline · **47/47 + batería completa verde** (f2 66/66 · f3 42/42 · f4 175/175 · f5 80/80 · misiones 34/34 · tripulaciones 38/38 · cibernética 31/31 · ficha 31/31) · `check-inline-styles` limpio · `sync-theme verify` OK. **Los 67 trámites tienen efectos** — queda la auditoría transversal de F6.

### D6.1 · La IA es invisible para el jugador (2026-08-28)
- **Decisión:** en NINGÚN momento se le dice al usuario (jugador) que algo lo desarrolla/propone/narra una IA. La intervención de la IA (y las skills/prompts) es información interna del proyecto, conocida solo por el WM.
- **Ámbito:** todo lo visible al jugador — `Manual_del_Jugador.md` y el front (páginas `tramite-NN.php` vía `paginas.php`, wizard de creación, ficha, hubs). Los documentos internos (Manual del Staff, OPERACION-STAFF, AGENTS, REGISTRO, zona staff) conservan la jerga operativa (prompt/skill/IA) porque son la herramienta del staff.
- **Redacción visible:** el trabajo se atribuye al «staff», «el equipo de moderación» o «el foro» (el foro estudia, el staff decide, el resultado vuelve a ti, el staff construye/firma).
- **Aplicado:** 15 menciones limpiadas en `Manual_del_Jugador.md` + 15 hints/formularios en `inc/ope_rol/tramites/paginas.php` (las «máquina» de cibernética son lore temático, se conservan).

### Cierre F6.2 · Auditoría transversal de cierre (checklist §2) (2026-08-28)
- **Trámite 19 (Reclutamiento de NPC, 12.5) — el único sin efecto** (`[PENDIENTE]` en el motor): módulo `inc/ope_rol/sistemas/npc.php` — `ope7_efecto_reclutar_npc` busca la ficha existente en `bestiario` por nombre (usa fichas existentes, sin ficha de combate, 12.5), marca «reclutado» en `npc_apariciones` (manejado_por = reclutador; `estados` = `{}` por el bug MyBB null→'' en JSON NOT NULL) + case 19 en el motor + require en bootstrap. El catálogo ya lo tenía como ligero/firma.
- **Efectos de mundo del trámite 62 aplicados de verdad (D2.2 cerrado)**: además de la fruta renacida (5.18) que ya existía, al firmar la muerte ahora se retira el cartel vigente/frío (`estado = retirado`, si `wanted_base > 0`, 5.13), se da de baja la facción con histórico (`ope7_faccion_registrar`, 5.12) y se inserta el **suceso de ronda `muerte`** en `sucesos` (ronda activa, 5.14). El mensaje del efecto ya no dice «pendiente de F4».
- **3 paneles A.3 que faltaban (18/18 completos)**: `mercado-staff.php` (`ope7_mercado_panel_html` en tiendas.php: fluctuación por zona/ronda con motivo + carteras robable/bóveda + transacciones) · `npc-staff.php` (`ope7_npc_panel_html` en npc.php: primarios con capa oculta solo-staff + bestiario + apariciones por tema incl. «reclutado») · `reliquias-staff.php` (`ope7_reliquias_panel_html` en muertes.php, nuevo require en bootstrap: fichas muertas con leyenda + histórico con calidad/herencia). Cards en Zona Staff + scopes CSS `ope-pg-mercado-staff`/`ope-pg-npc-staff`/`ope-pg-reliquias-staff`.
- **`docs/OPERACION-STAFF.md` escrito** (pendiente del done §2): guía operativa diaria — bandeja (firma con motivo), ronda de Mundo Vivo (visibilidad manual del periódico), panel por panel del A.3, las 8 skills con cuándo usarlas, números sagrados y reglas (sin dados, sin canon, no-crunch).
- **Auditoría del resto del checklist**: los 67 trámites con case en el motor · los `rand()` solo en `posthash` técnico (los «dados» de `core/data.php` son texto del motor viejo conservado, sin uso en el motor nuevo) · sin canon (no hay personajes/eventos canon como contenido) · Zona B con validación de PA ✓ · migraciones idempotentes ✓ · `check-inline-styles` limpio · `sync-theme verify` OK.
- Verificado por test `scripts/test-7seas-bajomundo.php` ampliado a **60 checks**: [11] trámite 19 — crea solicitud, sin bestiario bloquea (usa ficha existente), con bestiario ad hoc firma OK y aparición «reclutado» por el reclutador; [12] los 3 paneles nuevos renderizan sin inline. Idempotente (2 corridas consecutivas 60/60). Batería completa verde (f2 66/66 · f3 42/42 · f4 175/175 · f5 80/80 · misiones 34/34 · tripulaciones 38/38 · cibernética 31/31 · ficha 31/31 · bajomundo 60/60) · `check-inline-styles` limpio · `sync-theme verify` OK. **Checklist del §2 completo: 67 trámites operativos · 18 paneles A.3 · OPERACION-STAFF.md · sin dados · sin canon · migraciones idempotentes.**

### Cierre F5.5 · Despertar (48) y adaptación de fruta bajo demanda (49) — sistema de frutas completo (2026-08-28)
- **Efecto 48 · Despertar** (`ope7_efecto_despertar_akuma`, ia + firma): validación dura al firmar — fruta comida (`personajes.akuma_id`, trámite 47), **banda de nivel por tier/familia** (números cerrados 19.4: T1–T2 nv25 · T3–T4 nv32 · Logia/mitológica nv40, helper `ope7_akuma_nivel_despertar`) y **no despertada antes** (tabla `despertares`, una vez por fruta y por portador). Antigüedad on-roll (`ope7_akuma_meses_portador`, desde `akuma_historico` × ratio del calendario 5.6), temas cerrados (`ope7_akuma_temas_cerrados`, trámite 2 publicado) y VOL se reportan al staff para la firma (la IA propone, el staff decide). Registra en `despertares` y **genera suceso de Mundo Vivo en borrador** (5.14) — la Logia siempre es periódico.
- **Efecto 49 · Adaptación bajo demanda** (`ope7_efecto_adaptar_akuma`, staff, skill-adaptacion-akumas): el staff inicia con concepto (nombre+concepto canon) desde el panel; la skill construye la ficha de 8 bloques (prompt 49 nuevo); el staff la edita en la bandeja y al firmar el efecto **da de alta la fruta en `akumas`** (raw SQL como el seed — MyBB convierte null→'' y el ENUM `rareza` no lo acepta) + **objeto «Fruta: X»** (categoria akuma) para que circule por las tres vías (45/46/recompensas). Validación: nombre obligatorio y no duplicado (fruto único, cupo mundial 19.7), familia y tier 1–5.
- **Panel «Akumas y Haki» ampliado:** sección «Sucesos en borrador» ahora cubre `haki`+`akuma` (48 y 50), nueva sección **«Fruta bajo demanda»** con el formulario que crea el trámite 49 y nueva sección **«Despertares»** (histórico 48 con fruta/portador/trámite). Formulario 48 en `paginas.php` (el 49 es solo-staff, sin ventanilla).
- Verificado por test `scripts/test-7seas-f5.php` (bloques [4b]/[4c] + panel): sin fruta → firma rechaza · nv24 T1–T2 → firma rechaza (banda) · nv26 → publica + `despertares` + suceso borrador · repetido → rechaza · 49 alta en catálogo + objeto + duplicado rechaza · **80/80 + batería completa verde** · `check-inline-styles` limpio · `sync-theme verify` OK.

### Auditoría de la ficha — bloques F5 y desglose auditable (2026-08-28)
- **Bloque «Fruta del diablo»** verificado: campos vs DDL (`nombre_propio`/`familia`/`tier`/`rareza`/`aspecto`/`mecanica_base`/`puertas`/`debilidades`/`despertar`/`influencia_ficha` — todos existen y se decodifican en `ope7_akuma_info`); lógica `sin_comer` correcta (si `akuma_id < 1` busca `portador_id = PJ AND estado = 'con_portador'` → muestra «sin comer — trámite 47» sin mecánica base); afinidad −10 % PE mostrada; influencia (dotes/defectos de la ficha) consistente con el efecto 47. El efecto 47 no cambia `estado` (sigue `con_portador` tras comer: correcto, el cupo persiste).
- **Bloque «Haki»** verificado: tipos/nivel/`usos_acumulados`/`pp_invertidos`/`activo` coinciden con el DDL; el conteo de usos al cierre de tema (`ope7_haki_contar_usos_cierre`, +1 por tipo) alimenta la columna que muestra la ficha.
- **Bloque «Tripulación» AÑADIDO** (no existía): banda activa con nombre, miembros (capitán con 👑), propósito y **cofre común (5.9)** con saldo — reutiliza `ope7_pj_tripulacion_activa`/`ope7_trip_get`/`ope7_trip_miembros`/`ope7_trip_cofre_get`; clases f7 existentes, cero CSS nuevo.
- **Bloque «Misión en curso» AÑADIDO** (no existía): auto-narrada activa (`estado='en_curso' AND solicitante_id = PJ`) con nombre, **tramo/acto** (`ope7_mision_ultimo_tramo`), **condiciones de victoria/fracaso** explícitas y aviso del trámite 53 (21.3).
- **Desglose auditable intacto:** atributos base + racial (solo raciales en `ope7_pj_modificadores_efectivos` — los implantes no se mezclan en el desglose, sus bonos van en su propio sistema), secundarios calculados, dotes/defectos con **origen visible** (narrativo de implantes/linajes no confunde la balanza de creación), dominios, técnicas y sección solo-staff. Sin estilos inline.
- Verificado por test `scripts/test-7seas-ficha.php` (22 checks): fruta comida (nombre/familia/tier/mecánica/influencia, sin «sin comer») + variante sin comer (con aviso, sin mecánica) + Haki (tipo/nivel/usos/PP) + tripulación (nombre/miembros 👑/cofre 42.000 ฿) + misión (nombre/tramo 2/3/acto 2/condiciones) + desglose (base+racial, secundarios, origen narrativo, dominios, técnicas, solo-staff, sin inline) · **22/22 + batería completa verde** (f5 64/64 en corridas limpias — el 63/1 inicial fue residual por la akuma asignada/restaurada del catálogo) · `check-inline-styles` limpio · `sync-theme verify` OK.

### Auditoría de paneles staff contra el Anexo A.3 (2026-08-28)
- **Mundo Vivo (A.3 «recompensas con motivo» + «periódico News Coo»):** AÑADIDAS dos secciones al panel — «Recompensas con motivo» (lee `recompensas_historico`, últimas 15 con tipo/cantidad/ronda/motivo) y «Periódico News Coo» (lee `historico_periodicos`, últimas 5 con edición/estado, botón **Publicar edición** para borradores → `gaccion=publicar_periodico` en `mundo-vivo.php` con `verify_post_check`; visibilidad manual 15.2, alineado con el patrón de sucesos del Conquistador).
- **Facciones (A.3 «sueldos y nóminas»):** AÑADIDA la sección «Sueldos y nóminas» (lee `sueldos` por ronda con posts/monto/estado; vacía hasta que el cron de sueldos genere filas — vista honesta, sin inventar pagos).
- **Akumas (A.3 «catálogo con la plantilla de 8 bloques»):** AÑADIDA la sección «Ficha de 8 bloques» — `<details>` por fruta con los 8 bloques (aspecto, mecánica, puertas, debilidades, requisitos, influencia, despertar, precio/PP) decodificados del JSON real.
- **Cibernética (A.3 «mantenimientos pendientes por ronda»):** AÑADIDA la sección «Mantenimiento por ronda» — por implante activo/averiado: coste (×2 con «Mantenimiento oneroso»), último pago (de `implante_historico` evento `mantenimiento`) y situación derivada: **AVERIADO** (paga para reparar) / **pendiente** (sin pago desde el inicio de la ronda activa) / al día. Sin inventar números: todo sale del histórico real.
- **Correcciones menores:** mensaje vacío de narradores corregido a la fuente canónica de cuentas (21.2). CSS: `.zs-tab` añadido al scope de facciones y `.zs-ficha`/`.zs-bloque` al de akumas; el resto reutiliza clases existentes (`mv-*`, `zs-*`, `ms-*`).
- **Verificado:** sintaxis de los 5 archivos tocados · `check-inline-styles` limpio · `sync-theme verify` OK · batería completa verde (f2 66/66 · f3 42/42 · f4 175/175 · f5 64/64 · misiones 34/34 · tripulaciones 38/38 · cibernética 31/31 · ficha 31/31).

### F6.3 · Retirada por capas del esquema de la era anterior (2026-08-28)
- **Decisión (consultada, opción recomendada):** retirada **por capas** — se migra primero todo lo que el motor 7 Seas toca en vivo y se retira/archiva solo lo huérfano; el plugin viejo (4.500 líneas, da navbar/head/postbit a todas las páginas) se **conserva intacto** con sus guards `table_exists()` como legado documentado, hasta una futura reescritura. Nada se DROP: las tablas se archivan con una operación `RENAME` a un prefijo archivado (reversible).
- **`mybb_ope_cuentas` creada** (A.1, sustituye la antigua tabla de cuentas como fuente canónica): puntero de personaje activo + roles staff/narrador + slots. Migración idempotente de datos: `INSERT ... SELECT ... ON DUPLICATE KEY UPDATE` desde la antigua tabla de cuentas (solo si la tabla legada existe).
- **Código migrado a la fuente canónica** (con fallback al legado si la tabla nueva no existe): `ope7_pj_activo`/`ope7_pj_set_activo` (`dominio/personajes.php`, escribe en `ope_cuentas` + espejo en la antigua tabla de cuentas para el plugin) · `ope7_permisos` (`core/permisos.php`, staff/narrador leen de `ope_cuentas`, sin depender de `ope_rol_active_staff` del plugin) · `ope7_bot_personaje` (`core/bot.php`, bot «OPE Eternal» en `ope_personajes` uid=2 `es_NPC=1`) · narradores de misiones (join `ope_cuentas`/`ope_personajes`) · bibliotecas públicas `ope_rol_cat_personajes_publicos`/`ope_rol_cat_npcs_publicos`/`ope_rol_cat_fichas_query` (`catalogos/gestion.php`, leen de `ope_personajes` con join de razas/facciones).
- **Bug real corregido en la migración:** `ope7_tabla_existe_legacy` concatena el prefijo del esquema antiguo + nombre, así que la llamada con un nombre de la era anterior buscaba la tabla con doble prefijo y la copia de datos nunca se ejecutaba — corregido a `'cuentas'`. Detectado porque `ope_cuentas` quedó vacía tras la primera corrida.
- **Archivado de tablas muertas (F6.3):** las tablas que solo usaba el motor viejo (catálogos, mundo vivo, cartas, trámites, etc.) se archivaron de forma reversible y, tras el drop físico D6.6, quedaron eliminadas de la BD. No se conservó ninguna referencia a esos nombres en el código; los hooks activos del plugin leen la fuente canónica `mybb_ope_*`.
- **Verificación:** batería completa verde (wizard DONE · f1-tramites DONE · f1 DONE · f2 DONE · f3 DONE · f4 DONE · f5 80/80 · ficha 31/31 · misiones 34/34 · tripulaciones 38/38 · cibernética 31/31 · bajomundo 60/60 · motor DONE) · permisos admin/bot desde `ope_cuentas` · bot resuelto (id 2024) · bibliotecas leen de `ope_personajes` (1 NPC = bot) · paneles staff renderizan · `check-inline-styles` limpio · `sync-theme verify` OK · migración idempotente (2.ª corrida sin efectos).

### Cierre F6.3-código · Reescritura del plugin a la fuente canónica `mybb_ope_*` (2026-08-29)
- **Decisión (ejecutada):** con la decisión F6.3 ya tomada (retirada por capas, plugin conservado), se completa la reescritura **del código vivo** del plugin `inc/plugins/ope_rol.php` (4.645 → ~3.750 líneas) y de los arriendos vivos a la fuente canónica `mybb_ope_*`. **Objetivo:** retirar las 34 tablas el esquema de la era anterior restantes del código (los guards `table_exists()` quedan solo como código muerto inerte).
- **Plugin (`inc/plugins/ope_rol.php`):** flipeados a canónico los fallbacks de `is_installed`, `ope_rol_global` (cuentas/personajes), el resume de la navbar y la cadena del postbit (snapshot → canónico); **eliminadas solo funciones muertas de verdad** (sin `add_hook` ni callers en el árbol vivo), ~900 líneas. **Errata de proceso corregida:** la primera pasada borró por error 11 funciones que YES estaban referenciadas por `add_hook` legacy/comentados (`ope_rol_snapshot_post`, `ope_rol_save_thread_meta`/`_edit`, `ope_rol_parse_rpg`/`_mision`/`_viaje`, `ope_rol_cu_on_post`, `ope_rol_mision_showthread_end`, `ope_rol_viaje_showthread_end`); el check de «functions definidas por cada hook» (`add_hook` → definición) las detectó y se restauraron íntegras desde HEAD (razón: un función `add_hook`-eada no es muerta, aunque no tenga callers directos; las devolvimos definidas con sus hooks comentados como referencia legacy, sin activarlas). Estado final del plugin: **20 `add_hook` activos** (+ el inyector Zona B vía `pre_output_page` por la tarea posterior de QA) y ~3.750 líneas.
- **Helpers del postbit (Capa B):** `ope_combat_estados` → `ope_estados` · accessors de fruta/Haki (`ope_fruta_pj`/`ope_fruta_by_id`/`ope_fruta_lista`/`ope_haki_all`/`ope_haki_can_level`/… en `sistemas/frutas.php` y `sistemas/haki.php`) leen de `ope_personajes` (datos JSON) en vez de la antigua tabla de personajes; `ope7_*` para lo nuevo.
- **Páginas front (Capa C):** `gestion.php` (hub del navbar, fuente `ope_personajes` + `ope_barcos`), `ope_forum_meta` (espejo `mybb_ope_forum_meta` + lectura en `inc/ope_functions.php`), `ficha.php` (resolver de personajes, blindada la llamada a `ope_rol_tecnicas_de` en el bloque legacy inalcanzable), `progresion.php`/`crear-personaje.php` (cupo de slots desde `ope_cuentas`), `catalogo-npcs.php` (bestiario canónico `ope_personajes`/`ope_faccion_personaje`), `barco.php` (reescrita como **visor de flota canónico** `ope7_barco_flota`/`ope7_barco_por_id`, gestión enlaza a `astillero.php`; borrada la maquinaria legacy de fotos/cofre/invitaciones que tocaba `rol_barcos` archivada), `resumen.php` (stats/inventario/barco/trámites a canónico), `tienda-personal.php` (CRUD de inventario a `ope_inventario`), `tripulacion.php` (fuente `ope_tripulaciones` + aviso honesto: el ingreso lo inicia el capitán con el trámite 64).
- **Catálogos (`inc/ope_rol/catalogos/gestion.php`, Capa D):** `ope_rol_cat_nombre_pid` a `ope_personajes` (única función viva con ref legacy en ese archivo) · **bug real corregido:** la tabla canónica de rangos es `rangos_faccion` (no `facciones_rangos` como referenciaba `ope_rol_cat_fichas_query`) · `ope_rol_cat_lore`/`ope_rol_cat_tripulacion_miembros` quedan como código muerto con guard (sin callers).
- **Migración/esquema (Capa E):** `migrate-7seas-esquema.php` crea el espejo `mybb_ope_forum_meta` desde la antigua tabla de metadatos del foro (ya archivada), idempotente. **BD local: 118 tablas `mybb_ope_*` y ninguna del esquema anterior (sin tablas viejas en BD) activas** — las 54 están como las tablas archivadas de la era anterior.
- **Restos legacy en código muerto/guardado (aceptados):** módulos viejos que el bootstrap aún `require_once` (`mundo/mundo.php`, `mundo/viajes.php`, `mundo/misiones.php`, `mundo/barcos.php`, `oraculo_post.php`, `tramites/tramites.php`, …) y funciones de `system.php` sin callers vivos (`ope_pp_buy_stat`, `ope_combat_recalc`, `ope_rol_pj_aprobar/rechazar`, `ope_system_pid`, `ope_rol_cat_*` sin uso) — todos con guard `table_exists()` → no-ops inertes tras la retirada; verificado que **ninguna tiene callers en el árbol vivo** (hooks del plugin ya flipeado).
- **Verificación:** `php -l` limpio en los 21 archivos · batería completa verde (**13/13**: f1 · f1-tramites · f2 · f3 · f4 · f5 · ficha · misiones · motor · regla9b · tripulaciones · cibernética · bajomundo) · wizard exit 0 · `check-inline-styles` limpio · `sync-theme verify` OK · BD: `ope_cuentas`/`ope_forum_meta` presentes, 0 legacy activas.
- **Estado final:** la deuda documental del legado el esquema de la era anterior **queda como código muerto con guard**; las 54 tablas están retiradas en BD. Pendiente futuro: **borrado físico / drop reversible** de las las tablas archivadas de la era anterior (ya sin código vivo que las use).
- **Bugs estructurales del plugin corregidos en runtime (2026-08-29):** el QA visual real en XAMPP destapó dos roturas introducidas por la restauración F6.3: (1) el docblock de `ope_rol_snapshot_post` quedó **sin `*/` de cierre**, y como un `?>` dentro de un comentario `/* */` cierra el modo PHP, el `?>` del regex `OPERPGSYS` (L1228) convertía **todo el resto del archivo (L1228–3757) en HTML inline** → `ope_rol_theme_js()` indefinida en la portada (fatal en `index.php`); (2) `ope_rol_showthread_tags()` quedó **sin su `}` de cierre**, anidando todas las funciones restauradas (snapshot_post, parse_rpg, cu_on_post, parse_viaje, parse_mision, viaje_showthread_end, mision_showthread_end, inject_zonab_editor) dentro de su cuerpo — solo se definían si esa función se ejecutaba. Corregido: `*/` añadido, `}` de cierre añadido y la `}` huérfana del final eliminada. Verificación: tokenizador llega a L3757, 94 funciones a nivel superior, harness renderiza la navbar con `ope-theme-js`, batería **13/13** + wizard 0 + `check-zona-b` 15/15 + `check-inline-styles` limpio + `sync-theme verify` OK; barrido de PHP puro del repo sin más close tags ni cortes. **Blindaje nuevo: `scripts/check-parseo.php`** (sin BD): verifica que el PHP puro del motor se tokeniza hasta la última línea, sin close tags `?>` ni funciones nombradas anidadas (eximiendo guards `if (!function_exists(...))`); validado contra el plugin roto real de git (`5fcfcf0`, 3 reglas disparadas: close tag + HTML inline + cierre temprano en L1228/3757) y con smoke 3/3 (close temprano exit 1, anidada exit 1, guard exit 0) — 218 archivos / 1018 funciones OK. **Auditoría por commits de la restauración:** extraídos del git y analizados en su estado original todos los archivos PHP de `5fcfcf0` (21) y de los commits anteriores `1b67072`/`a68f734`/`d54e759`/`fafcd57`/`b2629d2` (179 más) — el **único** con daño estructural es el plugin en `5fcfcf0` (ya corregido; estaba limpio en todos los commits previos), confirmando que el daño lo introdujo la propia restauración y que ningún otro archivo lo arrastra. Bonus de limpieza: BOM UTF-8 retirado de `sistemas/enlace.php`, `sistemas/pl.php`, `scripts/rebrand-gbe.php` y `scripts/restructure-skydoms.php` (el BOM antes de `<?php` es un bloque HTML inline).
- **Limpieza de contenido de la BD de desarrollo (2026-08-29):** nuevo script `scripts/limpiar-contenido.php` (idempotente, convención `_db-config.php`): borra **todo el contenido** — personajes de prueba, temas, trámites, carteras, histórico PP, turnos de combate, `atributos_secundarios`, hilos/posts de MyBB — y **conserva** catálogos/semillas (islas, akumas, razas, dotes, facciones…), usuarios (`admin` + bot) y el personaje del bot «OPE Eternal» (se recrea vía `ope7_bot_personaje()` si falta; tras la limpieza quedó como PJ #23031 con uid 2). Reinicia contadores de foros, `personaje_activo` de cuentas y purga la datacache (stats/forums/latest_activity/ope_home). Aplicado: BD de desarrollo **limpia de contenido** (verificado: ope_temas/tramites/carteras/historico_pp/turnos_combate/threads/posts/atributos_secundarios = 0, personajes solo el bot, usuarios 2; catálogos intactos: 17 islas, 34 zonas, 18 akumas, 11 razas, 29 dotes, 20 defectos, 8 facciones, 56 objetos, 3 implantes). **Nota de idempotencia y corrección (2026-08-29):** la batería de tests **re-sembra personajes de prueba** ("Prueba Ficha", "Prueba Trip cap", "Prueba Cib uno"…) — si se re-ejecuta tras limpiar, reaparecen; hay que volver a correr el script. Además, algunos tests crean personajes con **el uid del bot (2)** pero `es_NPC = 0`, así que el borrado de personajes filtra por `NOT (uid = 2 AND es_NPC = 1)` (conserva solo el bot real), no por uid. El script también vacía el contenido de las 54 tablas las tablas archivadas de la era anterior (respaldo D6.3: se conserva su estructura, no su contenido).
- **Revisión de `require_once` muertos en `bootstrap.php` (2026-08-29):** retirados los **22 requires** de módulos viejos — `mundo/*` (mundo, oraculo, oraculo_post, viajes, islas_cat, matriz_rutas, barcos, nav_items, oraculo_v2, viaje_ai, viaje_cola, viaje_revision, misiones, mision_oraculo, mision_ai, mision_post, viaje_revision_ai), `tramites/tramites.php` y `sistemas/{enlace,renombre,pl,rachas}` — quedando solo el motor 7 Seas. Auditados previamente los callers: los invocados son código muerto (hooks `parse_viaje`/`parse_mision`/`showthread_end` de viaje/misión comentados) o guards inertes (`function_exists`). Single migración del caller vivo: la sidebar navega de `ope_isla_por_slug` → `ope7_isla_por_slug` (canónico). `revision-viaje.php` (página staff huérfana del sistema viejo, sin enlace y con redir a `zona-staff-viajes.php` inexistente) se autoabastece del trío de viaje vía requires directos para no romperse. Batería **13/13** + wizard 0 + `check-inline-styles` limpio + `sync-theme verify` OK + `check-zona-b` 15/15.

- **Drop físico del legado D6.6 (2026-08-29):** borrado total del esquema de la era anterior — **las 54 tablas de la era anterior fueron DROPEADAS** (ninguna del esquema anterior en BD) y se eliminaron del repo: los **17 stubs** `inc/ope_rol_*.php` (D0.4), los **17 módulos legacy** `inc/ope_rol/mundo/*`, `sistemas/{enlace,pl,rachas,renombre}.php`, `tramites/tramites.php`, `config/viaje_ai.sample.php`, la **página huérfana `revision-viaje.php`** (sin enlace, redir a `zona-staff-viajes.php` inexistente), la **task legacy `inc/tasks/ope_viajes.php`** (+ su fila en `mybb_tasks`, estaba enabled) y **4 scripts muertos** que consultaban tablas retiradas (`inspect_schema`, `seed-mundo-vivo-demo`, `verify_npcs_ficha`, `verify_staff_cards`). Callers vivos migrados a rutas canónicas: `ficha.php`/`personajes.php`/`zona-staff.php` → `core/data.php` + `core/system.php` (eran los stubs); **bloque «Renombre» retirado de la ficha** (módulo `sistemas/renombre.php` leía la antigua tabla de renombre, ya eliminada — era la última UI legacy viva). Verificación: `php -l` limpio · `check-parseo` (219 archivos OK) · batería **13/13** · wizard 0 · `check-zona-b` 15/15 · `check-inline-styles` limpio · `sync-theme verify` OK · grep final: sin callers duros a funciones legacy (solo guards `function_exists`/`table_exists` que no-op). El comentario del catálogo `pj.php` y el bloque 3b de `limpiar-contenido.php` actualizados (sin tablas retiradas que vaciar).
- **Purga total del código legacy D6.7 (2026-08-29):** tras el drop físico quedaban refs al esquema de la era anterior en el **código vivo** (con guards que hacían no-op) y en scripts muertos. Se eliminó **todo**: (1) **77 scripts muertos** de desarrollo (`check_*`, `inspect_*`, `migrate-*` de la era vieja, `wipe-*`, `seed-*` de demo…) que consultaban tablas del esquema anterior; (2) **9 funciones legacy del plugin** con hooks comentados (`ope_rol_snapshot_post`, `parse_rpg`, `cu_on_post`, `parse_viaje`, `parse_mision`, `viaje_showthread_end`, `mision_showthread_end` — bloque L2993–L3726); (3) **rama legacy de `ficha.php`** (−2.550 líneas: tras el `exit` de la rama 7 Seas, todo lo posterior era inalcanzable porque `$pj` siempre es `null`); (4) **funciones legacy de `core/system.php`** que leían la antigua tabla de personajes (`ope_system_uid/pid/create_*`, `ope_pp_word_table/for_words/count_words/add/on_post/buy_stat`, `ope_combat_recalc`, `ope_rol_pj_aprobar/rechazar` — −519 líneas); (5) **6 funciones muertas de `sistemas/frutas.php`** (`roll_aleatoria`, `liberar`, `add_cu`, `buy_level`, `despertar`, `sortear_aleatoria`) y **5 de `sistemas/haki.php`** (`ensure_row`, `buy_level`, `marcar_despertado`, `buy_ken_t1`, `add_cu`) — las vivas (`ficha_block`/`norm`/`row`/`all`/`can_level`) ya leían canónico; (6) **15 funciones CRUD de trámites / tripulaciones / cartas de la era anterior en `catalogos/gestion.php` (sin callers; −442 líneas) + el guard de la antigua tabla de personajes en `ope_rol_cat_fichas_query`; (7) **3 shims muertos de `catalogos/pj.php`** (`ope_rol_armas`, `ope_rol_virtudes`, `ope_rol_defectos`); (8) **sección F6.3/F6.4 del esquema** (copia de datos desde el esquema de la era anterior y RENAME reversible — tablas ya inexistentes) y el **script `migrate-7seas-f1.php`** (apuntaba a la antigua tabla de cuentas, ya dropeada); (9) **comentarios** que mencionaban tablas legacy (bibliotecas, bot, permisos, dominio, misiones, tienda-personal, tripulacion, `ope_user_init`, `barco`, `personajes`, plugin) y el bloque 3b muerto de `limpiar-contenido.php`. Verificación: `grep` de comillas y de palabras → **0 refs al esquema de la era anterior en todo el PHP** (solo quedan menciones históricas en la documentación); `php -l` limpio en todos los modificados; batería **13/13** + checks verdes.

### F4.2 · Panel staff «Progresión» (Anexo A.3)
- Página `progresion-staff.php` (scope `body.ope-pg-progresion-staff`, card en Zona Staff) con `ope7_progresion_panel_html()` en `sistemas/progresion_panel.php`: **cronómetros de entrenamiento** por jugador (bloque 5/10, fin real + días restantes, aviso de que al vencer entran en reserva — 7.3) · **saldos y reservas** (tabla por PJ: nivel, PP saldo, reserva, comprados desde el último nivel, lo que falta para el siguiente) · **gastos de PP por concepto** (agregación del libro `historico_pp`: atributos, dominios, técnicas, cierres) · **histórico reciente** (últimos 25 movimientos con fecha y motivo).
- **Completado del libro de PP:** el efecto 4 (compra de PP, ligero) **no registraba el gasto** en `historico_pp` — ahora registra `cantidad`/`concepto` («Compra de PP: bloque de N en atributo», con `tramite_id` del ligero). El cierre de tema (efecto 2) también completa su fila con `cantidad`/`concepto` para que el panel agregue ingresos y gastos por concepto coherentemente.
- Verificado por test (bloque [4b]): compra registra el gasto con concepto, panel muestra el cronómetro del PJ, el gasto en negativo y la tabla de saldos · 42/42 + batería completa.

### D4.6 · Precio de la ración (17.6) y panel «Navegación» (A.3)
- **Precio de la ración (número sagrado):** el manual 17.6 dice 50 ฿ y el seed la tenía a 100 ฿. **Decisión: 50 ฿ (manual).** `seed-7seas-progresion.php` corregido (idempotente) con nota (D4.6); el ejemplo del manual (4 personas, Blue→Paraíso, 2 medios ≈ 2.800 ฿) cuadra con 50.
- **Panel staff «Navegación» (`navegacion-staff.php`, scope `body.ope-pg-navegacion-staff`, card en Zona Staff)**: `ope7_navegacion_panel_html()` en `sistemas/navegacion.php` — travesías activas por jugador (ruta con regiones, medio barco/transporte, plazo + días on-roll, **vencimiento** real con aviso <48 h, oráculos por tema, víveres al cierre) · aviso del flujo (ficha editable en la bandeja, IRT solo-staff) · histórico de resueltas/vencidas/abortadas con veredicto (víveres, daño al barco, notas).
- Verificado por test (bloque [4f]): panel con la travesía activa (PJ + ruta + oráculos) y la resuelta en el histórico, sin estilos inline · 87/87 + batería completa.

### D4.5 · Multiplicador de dominios adicionales (5.3/4.4)
- **Pregunta (F4.3):** el manual permite dos lecturas del multiplicador de coste de dominios adicionales. **Decisión:** anclado al dominio adicional — ×1,5 el 1.º adicional, ×2 el 2.º+; se fija al adquirir (`coste_mult` en `dominios_personaje`) y aplica también a las subidas de ese dominio. Los dominios de creación (wizard) siempre ×1,0. La creación de personaje no se ve afectada (presupuesto de creación, no de progresión).
- Implementado en `ope7_dominio_mult_adicional()` + `ope7_efecto_compra_dominio()` (trámite 4, ligero, rama dominios): coste = base del nivel objetivo (60/120/240/400) × multiplicador; valida nivel mínimo del PJ (10/20/35/45), cupo INT (1 adicional por 50 INT) y un solo dominio en entrenamiento a la vez (cronómetro independiente del de atributos, 4.4). Arranca el cronómetro de 15 días; al vencer, `ope7_pj_finalizar_dominios()` sube al nivel objetivo y limpia.

### Cierre F4.1 (2026-08-27)
- **Semilla del mundo** (`scripts/seed-7seas-mundo.php`, idempotente): 7 mares · **17 islas del catálogo 5.14** con ficha viva (13 parámetros: peligrosidad, afiliación, fuerza defensiva con quien manda, desarrollo, población, recursos, oferta/demanda, clima/log pose, lugares clave, sucesos, hitos, tesoros, facciones) + histórico de arranque · 34 zonas clave · 8 tipos de barco × N1–N3 · 5 maderas · 10 módulos · 7 oráculos · 3 transportes · **8 facciones jugables con escalera de rangos** (44 rangos).
- **Motor de ronda** (`inc/ope_rol/sistemas/mundo.php`): `ope7_ronda_abrir_siguiente()` · cola de presentes (`ope7_ronda_temas_pendientes`) · `ope7_ronda_cambiar_estado()` (abierta→análisis→cerrada) · **`ope7_ronda_aplicar_cierre()`** aplica lo firmado: cambios de isla con motivo e histórico (`isla_estado_historico`, fuente `ronda`), recompensas (`recompensas_historico`), precios dentro de banda 0,5×–2× (`precios_mercado` con ronda y fecha foro) y periódico **en borrador** (visibilidad manual, 15.2). Helpers de mundo: `ope7_isla_por_slug/id`, `ope7_isla_ficha`, `ope7_isla_zonas`, `ope7_isla_actualizar` (con motivo e histórico).
- **Panel staff «Mundo Vivo»** (`mundo-vivo.php`, scope `body.ope-pg-mundo-vivo`, card en Zona Staff): ronda actual con estado, cola de análisis (temas presentes), matriz de islas con ficha viva y aviso del flujo (skill propone → staff firma → motor aplica).
- **Tienda ↔ isla (D4.4):** apertura exige isla + territorio + Comerciante; `zona_id` = isla; precio anclado al mercado de su zona; `ope7_tiendas_suspender_en_isla()` (la usará la conquista F4.2). `tiendas-personales.php` con selector de isla del catálogo.
- **Tests `test-7seas-f4.php` 36/36** (semilla, ronda completa, cierre firmado con motivo, tienda↔isla, suspensión, panel) + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK. *(Con el panel «Progresión» F4.2: 42/42 · con la reserva en la ficha F4.2-bis: 47/47 · con el cronómetro de dominios F4.3: 58/58 · con navegación (trámite 38 + panel + D4.6): 87/87 · con facciones (19–24 + panel + D4.7): 109/109 · con conquista (34–37 + panel + D4.8): 146/146 · con barcos (39–44 + panel + D4.9/D4.10): 175/175.)*

---

## Mapeo del esquema canónico → Anexo A (auditoría F0)

> Las tablas de la era anterior ya no existen (retiradas e eliminadas en D6.6, 2026-08-29). El esquema vivo es exclusivamente el canónico `mybb_ope_*` del Anexo A.1; este mapeo histórico se conserva para explicar qué table del manual sustituyó a cada sistema.

> **Referencia por sistema:** personajes → `mybb_ope_personajes` (atributos en columnas) · trámites → `mybb_ope_tramites` + `mybb_ope_tramites_historico` (estados 5.21, firma, prompt) · permisos → `mybb_ope_cuentas` · mundo vivo → `mares`/`islas`/`isla_estado`/`rondas` · tiradas de dados: **sin equivalente** (principio 1).

**Incoherencias detectadas (sin aplicar cambios a reglas):**
- ~~El Anexo B del Manual del Staff lista 7 skills en su tabla, pero el cap. 21 anuncia «7 skills completas» y el cap. 23 añade la 8.ª (`skill-adaptacion-cibernetica`). El maestro del proyecto habla de 8 skills; se adopta el catálogo de 8.~~ **RESUELTO (2026-08-28):** el Anexo B se actualizó con las **8 skills completas** reflejando la implementación real (plantillas de `ope7_prompt_especifica`, trámites que las usan y guías maestras) — ver `Manual_del_Staff.md` → Anexo B.
- la antigua tabla de tiradas (con dados) (dados) existe en la BD vieja: se excluye del nuevo sistema por el principio 1; la exclusión se documenta aquí, no se borra la tabla.

---

### D1.8 (cerrada) · `ope_temas.tid` ↔ thread MyBB real (2026-08-28)
- **Cierre de la decisión abierta en F1:** nueva columna **`ope_temas.mybb_tid`** (INT, 0 = sin hilo aún, idempotente en la migración) que guarda el `tid` real de `mybb_threads`; `ope_temas.tid` sigue siendo el id interno del tema (los participantes lo referencian).
- **Vinculación automática en el hook de posteo** (`inc/plugins/ope_rol.php` → `ope_rol_after_thread`, `datahandler_post_insert_thread_end`): al crear un hilo real, `ope7_tema_vincular_mybb_por_pj` busca el tema **presente abierto del personaje autor que aún no tenga hilo** (`mybb_tid = 0`) y lo vincula — cubre apertura de tema (1), misión (52) y tripulación (63) sin tocar sus efectos. Se salta Off Topic (esos hilos no son temas de rol) y **nunca pisa** un tema ya vinculado (no reasigna).
- **Vinculación explícita**: el efecto 1 (apertura) acepta `mybb_tid` por formulario (`paginas.php`, campo opcional «ID del hilo del foro») o por resultado, para el caso de que el jugador postee el hilo antes de firmar el trámite.
- **Cierre del hilo real al cerrar el tema**: `ope7_tema_cerrar_mybb` marca `mybb_threads.closed = 1` cuando el tema pasa a `cerrado`. Conectado en los 3 cierres: efecto 2 (skill-cierre-temas), cierre de misión (55) y travesías vencidas (cron de navegación).
- **Helpers en `inc/ope_rol/tramites/motor.php`**: `ope7_tema_mybb_tid` (lectura) · `ope7_tema_vincular_mybb` (no pisa) · `ope7_tema_vincular_mybb_por_pj` (por autor) · `ope7_tema_cerrar_mybb` (cierra el thread).
- **Verificado**: test `f1-tramites` ampliado — [4] apertura con `mybb_tid=777` guardado + hook por_pj no pisa (queda 777) + [5] cierre con thread MyBB real (tid 777) que queda `closed='1'` · smoke end-to-end (tema sin hilo + thread real 4242 → vincula → cierra) · batería completa verde (f1-wizard DONE · f5 80/80 · ficha 31/31 · misiones 34/34 · tripulaciones 38/38 · cibernética 31/31 · bajomundo 60/60) · `check-inline-styles` limpio · `sync-theme verify` OK.

### D6.1-bis · Auditoría de mensajes del motor de trámites (2026-08-28)
- **Revisión completa de los mensajes de retorno visibles al jugador** (`msg` de `ope7_tramite_crear`, efectos al publicar, hub de trámites, ciclo con el usuario, libro de PP) en busca de jerga operativa (IA/skill/prompt) que contradijera la regla 9b (la IA es invisible para el jugador). **Corregido:**
- `motor.php` — concepto del libro de PP en cierres: `Cierre de tema (skill-cierre-temas)` → `Cierre de tema` (el libro de PP es visible al jugador, A.3 Progresión).
- `sistemas/misiones.php` — efecto 52: «tramo 1 creado (texto de la IA firmado por el staff)» → «tramo 1 creado y firmado por el staff» · efecto 53: «la IA nunca adelanta un tramo sin ellos» → «no se adelanta un tramo sin ellos».
- `sistemas/cibernetica.php` — efecto 59: mensajes «debe venir del resultado de la IA» y «calibrada por la skill» → «del resultado del staff» / «calibrada y firmada por el staff».
- `sistemas/akumas.php` — efecto 46 (concepto/concreta a bandeja): «la IA propone la fruta» → «el foro estudia la fruta».
- `tramites/catalogo.php` — `ope7_naturaleza_label('ia')`: `IA + firma` → `Revisión del staff (firma)` (etiqueta visible en las cards del hub del jugador).
- `tramites/bandeja.php` — hub del jugador: chip de filtro `IA + firma` → `Con firma del staff` · flujo del estado «pendiente → prompt → analizado» → «pendiente → en estudio → en revisión» · `ope7_tramite_estado_label('prompt_listo')`: `Prompt listo` → `En estudio` (visible en la lista del jugador).
- **Conservado a propósito (zona staff, D6.1):** la bandeja staff (`bandeja.php`, protegida por `ope7_es_staff`) mantiene «Prompt generado», «Resultado de la IA (editable)» y «la IA propone, el staff decide» · los paneles `*_panel_html` de cada sistema · los `ope7_tramite_log` internos del histórico staff.
- **Verificado:** `f5 80/80 · misiones 34/34 · cibernética 31/31 · f1-tramites DONE · motor DONE` · `check-inline-styles` limpio · `sync-theme verify` OK · grep final sin mensajes de retorno con IA/skill visibles.

### D6.1-ter · Test automatizado que blinda la regla 9b (2026-08-28)
- **Nuevo `scripts/test-7seas-regla9b.php`**: barré de regresión — falla (exit 1) si algún mensaje/texto **visible al jugador** contiene jerga operativa (IA/skill/prompt). Recorre: (1) páginas públicas de la raíz + la ficha 7 Seas (`dominio/ficha.php`), (2) el Manual del Jugador, (3) los strings de retorno del motor y de los efectos al publicar, y (4) la vista `ope7_tramites_jugador_html` del hub.
- **Excluye legítimamente (D6.1):** la zona staff — funciones `*_panel_html`, `bandeja_staff_html`, páginas `*-staff.php` y `bandeja.php` (protegida por `ope7_es_staff`) — y los comentarios de código `/* */` y `//`.
- **Violación real encontrada y corregida:** `tramites/bandeja.php` (vista jugador del hub) mostraba «ventanillas · motor 5.21» como subtítulo de `ope7_shead('Tus trámites', …)` → **«ventanillas del foro»**. Era jerga interna visible, confirmaba que la auditoría anterior no había barrido todas las superficies.
- **Calibrado y verificado:** el detector discrimina jerga de vocabulario normal (no falsea con «materiales y tienda»); inyectando una violación real en `crear-personaje.php` el test **falla** con la línea exacta, y revertida **pasa** (4/4, exit 0). `php -l` limpio en el test y en `bandeja.php`.

### Anexo B actualizado · 8 skills completas (2026-08-28)
- **Cierre del pendiente documental:** el Anexo B del Manual del Staff pasa de «resumen» de 7 skills a las **8 skills implementadas**, cada una con sus trámites reales del catálogo (columna nueva «Trámites que la usan»), su función según `ope7_prompt_especifica` y sus guías maestras: `skill-validacion-personajes` (3, ciclo con el usuario) · `skill-cierre-temas` (2/20/60/62/55) · `skill-creacion-tecnicas` (13) · `skill-mundo-vivo` (ronda/mercado/rumores/hordas) · `skill-navegacion` (38, IRT interno) · `skill-narracion-automatica` (52–55) · `skill-adaptacion-akumas` (45/46/48/49, guía 5.18) · `skill-adaptacion-cibernetica` (56/59, guía 5.22). Índice del manual actualizado (Anexo B: ✅ completo), línea del cap. 21 corregida (8 skills), incoherencia antigua del REGISTRO marcada RESUELTA y pendiente eliminado del AGENTS.md.

## Pendientes abiertos (con dueño y fase)

| Pendiente | Dueño | Fase |
|---|---|---|
| ~~Retirada/archivado de las tablas el esquema de la era anterior~~ (**HECHO** 2026-08-29: reescritura del plugin/páginas a `mybb_ope_*`; 0 tablas legacy activas en BD; restos en código muerto con guard) | — | — |
| 67 trámites: efectos «al publicar» específicos por sistema (hoy solo están los transversales: firma, histórico, posteo, notificación) | Implementador | F1–F6 |
| Hooks de A.2: registro progresivo por sistema (F0 solo implementa el motor llamable, no hooks de eventos) | Implementador | F1–F6 |
