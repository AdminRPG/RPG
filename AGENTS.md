# SUPER AGENTS.md — One Piece: 7 Seas (motor Eternal)

> **Documento maestro operativo del proyecto.** Cualquier agente lo lee **al empezar cada sesión** y lo mantiene **al día al cierre de cada fase y de cada sesión**. La deuda documental no se acumula.
> Repo hermano de sistemas (reglas de diseño): `c:/Users/Fgonz/Documents/Proyectos/Op-Eternal/Eternal-Sistema/docs/` (solo si se abre/referencia manualmente — no accesible por defecto desde este workspace).

---

## 1. Identidad

- **Producto / marca visible:** One Piece: **7 Seas** (foro de rol por escrito en el universo One Piece).
- **Motor (nombre técnico):** Eternal · prefijo de código PHP `ope_` (funciones, tablas `mybb_ope_*`) · prefijo CSS/plantillas `ope-`.
- **Plugin:** `inc/plugins/ope_rol.php` (codename `ope_rol`) · **Backend:** `inc/ope_rol/` (capas `core/`, `catalogos/`, `config/`, `dominio/`, `mundo/`, `sistemas/`, `tramites/`; entrada `bootstrap.php`).
- **Bot del sistema:** «OPE Eternal» (uid reservado 2) — News Coo, sucesos, rumores, posteos automáticos.
- **Prohibido reintroducir** `gbe_`, `gbe-`, Granblue, GBF, I-Forge o iforge en código nuevo (regla vigente).

## 2. Misión y objetivo final

> Que un jugador nuevo cree su personaje hoy y dentro de un mes esté navegando por el New World, peleando con cartas de técnica, ganando PP, y que el staff modere todo desde la zona staff con la IA como copiloto — fiel a los manuales, **sin dados** y sin crunch innecesario.

**Definición de done (checklist de lanzamiento, §12):** foro jugable de punta a punta (F6) · 67 trámites operativos · tablas del Anexo A.1 migradas · hooks de A.2 · paneles de A.3 · 8 skills integradas al flujo de trámites · formato de posteo en dos zonas con validación de PA · sin dados (2 excepciones solo deciden *qué obtienes*) · sin personajes/eventos canon · `check-inline-styles` limpio · `sync-theme verify` OK · migraciones idempotentes · `docs/OPERACION-STAFF.md` escrito · SUPER AGENTS.md y REGISTRO_DECISIONES.md al día.

## 3. Fuentes de verdad y orden de lectura

**`docs/sistema/` (los manuales) = ley de reglas.** Si un doc del repo (README, docs antiguos) contradice a los manuales, **ganan los manuales**. La identidad visual y las convenciones de código siguen el `DESIGN` y el `GUIA-ESTILOS-PHP.md`.

1. `docs/sistema/Manual_del_Staff.md` — **la fuente de implementación**: operativa + cap. 22 (67 trámites + prompts) + **Anexo A** (A.1 datos · A.2 hooks · A.3 paneles) + **Anexo B** (skills).
2. `docs/sistema/Manual_del_Jugador.md` — 23 capítulos; define la UX del front.
3. `docs/sistema/diseno/` — `5.14_catalogo_islas.md` (17 islas a sembrar) · `5.18_guia_adaptacion_frutas.md` · `5.22_guia_adaptacion_implantes.md` (guías maestras de skills).
4. Docs del repo: `README.md` · `docs/GUIA-ESTILOS-PHP.md` · `docs/DESIGN-ONE-PIECE-ETERNAL.md` · `docs/RESTAURAR-BACKUP.md` · `inc/ope_rol/README.md`.

**Regla de consulta por tarea:** antes de implementar un sistema, lee (a) su capítulo en el Manual del Staff (operativa + capa técnica + fila del Anexo A), (b) su capítulo en el Manual del Jugador, (c) la guía maestra si es fruta o implante. No implementes de memoria.

## 4. Principios innegociables (los 8)

1. **Sin azar.** Ninguna acción se resuelve con tiradas/dados. Únicas excepciones (deciden *qué obtienes*, jamás *qué pasa*): tirada del Haki del Conquistador (5.19) y tirada de akuma aleatoria (5.18, nv3+). No implementes ningún sistema de dados.
2. **Mundo Vivo (5.14) = pilar central.** Toda acción en un tema presente tiene consecuencias reales y medibles.
3. **Narrativa + comparación de mecánicas.** Los jugadores no dictan resultados; el veredicto lo cierra el staff con las tablas de delta al cierre del tema.
4. **Alta personalización.** Dos personajes con la misma raza y atributos se juegan distinto.
5. **Redacción explicativa.** Todo en el front bien explicado en prosa; las tablas apoyan, no sustituyen.
6. **Automatismos + no-crunch.** Los triggers condicionales los declara el jugador en su post y el staff los verifica en el momento; el sistema sugiere, nunca bloquea por falta de cálculo.
7. **Originalidad.** Sin personajes ni eventos canon como contenido. Los nombres canon (frutas, Haki, islas, facciones, «News Coo») sí se usan (News Coo: derogación puntual confirmada por el usuario).
8. **Números sagrados.** Fórmulas, precios, cupos y bandas cerrados. Ante incoherencia: anótala en `REGISTRO_DECISIONES.md` y consulta con pregunta guiada — no la cambies por tu cuenta.

## 5. Protocolo de trabajo guiado (IMPRESCINDIBLE)

- El agente **pregunta con opciones** (2–4, recomendada primero) en cada decisión consecuente y en cada fase; el usuario decide. **Máximo 4–5 preguntas por tanda**, priorizadas.
- **Reversible/menor** (variable, CSS, texto de botón) → elige la opción sensata, aplícala y avisa en una línea. **Consecuente** (arquitectura, números, flujo, alcance) → pregunta sí o sí.
- Si el usuario no responde, aplica la **recomendada**, señálalo al retomar y sigue — nunca te quedes bloqueado.
- Toda respuesta se anota en `docs/sistema/REGISTRO_DECISIONES.md` (fecha, decisión, opción, motivo, sistemas afectados). **Léelo antes de trabajar.**
- Al terminar cada fase: resumen + confirmación para avanzar (avanzar / corregir X / repasar Y).

## 6. Inventario del repo (actualizado a F0 · 2026-08-27)

**Ya existe (se conserva y se completa):**
- Portada `index.php` (scope `body.ope-index`): hero + bento + Los Mares (8 tarjetas vacías → se rellenan con el catálogo de 5.14) + Off Topic.
- Páginas: `ficha.php`, `personajes.php`, `crear-personaje.php`, `tramites.php` (hub del jugador), **56 ventanillas `tramite-NN.php`** (generador `scripts/generate-tramite-paginas.php`), `zona-staff.php`, `progresion.php`, `mapa.php`, `tiendas*.php`, `astillero.php`, `barco.php`, `tripulacion.php`, bibliotecas, `gestion.php`, `mensajes.php`, `alertas.php`, `resumen.php`, `guias.php` + **16 paneles staff** en `*-staff.php`: `zona-staff.php` (hub) + `akumas`, `bajomundo`, `barcos`, `calendario`, `cibernetica`, `conquista`, `facciones`, `familias`, `mercado`, `misiones`, `navegacion`, `npc`, `progresion`, `reliquias`, `tripulaciones`.
- **Nuevo en F5:** `inc/ope_rol/sistemas/akumas.php` (trámites 45/46/47/50, akumas + Haki) · `scripts/seed-7seas-akumas.php` (18 frutas + pool + objetos) · panel `akumas-staff.php` (A.3) · bloques «Fruta del diablo» y «Haki» en la ficha.
- Motor `ope_rol` (era anterior, **conservado, vivo a canónico tras F6.3-código**): `inc/plugins/ope_rol.php` (~3.000 líneas, 20 `add_hook` activos + navbar + staff por personaje; funciones `add_hook`-eadas restauradas íntegras tras limpieza; incluye el inyector del panel Zona B vía `pre_output_page`) + `inc/ope_rol/` (core, catalogos, dominio, sistemas, tramites). Los **17 stubs** `inc/ope_rol_*.php` (D0.4) y el directorio `mundo/` legacy fueron **eliminados** en el drop físico del legado (D6.6, 2026-08-29) y en **D6.7** se purgó el último código legacy (parsers con hooks comentados, CRUD de `rol_*`, shims muertos) — **cero refs a `mybb_rol_*` en el código** (solo quedan menciones históricas en la documentación).
- **Nuevo en F0:** `docs/sistema/` (manuales + diseno + `REGISTRO_DECISIONES.md`) · `docs/sistema/REGISTRO_DECISIONES.md` · esquema `mybb_ope_*` (Anexo A.1, decisión D0.3-bis) · motor de trámites 5.21 (`inc/ope_rol/tramites/{catalogo,motor,bandeja,paginas}.php`) · página `bandeja.php` · permisos sobre `mybb_rol_cuentas`.
- BD `rpg_forum` (PHP 8.3, MyBB 1.8.39): 118 tablas `mybb_ope_*` canónicas (verificado 2026-08-29) y **0 tablas `mybb_rol_*` en la BD** (las 54 `mybb_rol_retirada_*` del respaldo D6.3 fueron **DROPEADAS** en el drop físico D6.6, 2026-08-29). Entorno local `http://rpg.test/`, `MYBB_DB_NAME=rpg_forum`.
- Tema: `docs/themes/ope.css` (fuente de verdad) → sync con `php scripts/sync-theme.php import` / `verify`; `php scripts/check-inline-styles.php`.
- Scripts: migraciones/seeds en `scripts/` (convención `migrate-*.php`, `seed-*.php`, idempotentes, `_db-config.php`, `_migrate-lib.php`) · `sync-theme.php` · `check-inline-styles.php` · `restore-backup.php`.
- CI/CD: `.github/workflows/deploy.yml` → FTP a InfinityFree en cada push a `main`. **No desplegar manualmente.**
- Usuarios: `admin` + «OPE Eternal» (bot, uid 2).

**No tocar / no reescribir:** identidad visual del DESIGN · scaffolding de páginas (clases `.shead/.plate/.plate-h/.plate-b/.reveal/.flash/.pj-empty` **solo por scope** `body.ope-pg-<pagina>`; cero `<style>`/`style=""` estáticos; variables CSS solo las del sistema) · flujo de despliegue y backups · el legado `mybb_rol_*` (D6.3-F6.3-código + **D6.6 drop físico 2026-08-29**: el código vivo del plugin y páginas ya fue reescrito a la fuente canónica `mybb_ope_*`, los 17 stubs y módulos `mundo/*` fueron eliminados y las 54 `mybb_rol_retirada_*` **DROPEADAS** — y en **D6.7 (2026-08-29)** se purgó todo el código legacy restante (funciones muertas del plugin, módulos legacy de frutas/haki, CRUD de `rol_cards`/`rol_tramites` en `gestion.php`, shims de `pj.php`, sección F6.3/F6.4 del esquema, `migrate-7seas-f1.php` y 77 scripts muertos): **0 refs a `mybb_rol_*` en el código** — solo quedan menciones históricas en la documentación)

## 7. Arquitectura

- **Capas del motor (`inc/ope_rol/`):** `core/` (bootstrap, data, system, tablas, permisos) · `catalogos/` (datos puros en arrays) · `dominio/` (reglas puras) · `sistemas/` (combate, progresión, economía, haki, akumas, implantes, navegación, conquista) · `mundo/` (ronda, islas, sucesos, periódico, misiones, rumores, facciones, viajes) · `tramites/` (**motor transversal**: catálogo 67, motor, bandeja).
- **Nuevas funciones del motor 7 Seas usan prefijo `ope7_`** para no colisionar con el código viejo (`ope_`, `ope_rol_*`).
- **Patrón de página PHP (obligatorio):** `define('IN_MYBB',1)` + `require './global.php'` → `ope_rol_head_base()` → `ope_rol_navbar_html()` → breadcrumb → `<div class="wrap">` → `include inc/footer_custom.php` → IntersectionObserver. Scope CSS `body.ope-pg-<pagina>`.
- **BD:** tablas canónicas `mybb_ope_*` creadas por migraciones en `scripts/` (idempotentes). Nunca SQL a mano en producción. Respeta `docs/RESTAURAR-BACKUP.md`.
- **Cron/hooks (A.2):** calendario on-roll diario (1 real = 2 on-roll) · ronda del Mundo Vivo mensual · mantenimientos por ronda (implantes, espías, sueldos) · tripulaciones (<2 activos → disolución) · carteles (paradero 3 rondas). Doble cara: automático vs. lo que espera al staff (prompt → IA → firma).

## 8. Roadmap de fases (copia viva del maestro)

| Fase | Contenido | Estado |
|---|---|---|
| **F0** | Fundaciones: manuales en `docs/sistema/`, SUPER AGENTS.md, REGISTRO_DECISIONES.md, esquema Anexo A.1 (`mybb_ope_*`), motor de trámites + 67, permisos, hitos de bandeja/bot | ✅ completada (2026-08-27) |
| **F1** | Personaje: wizard de creación, skill-validacion-personajes, ficha completa, trámites 1–12 | ✅ completada (2026-08-27) |
| **F2** | Combate: PA/defensas/técnicas/estados, posteo en dos zonas, tablas de delta, muerte (trámite 62) | ✅ completada (2026-08-27) |
| **F3** | Progresión y economía: calendario, cierre de temas (skill-cierre-temas), carteras/tiendas, inventario | ✅ completada (2026-08-27) |
| **F4** | Mundo Vivo: ronda mensual, 17 islas, facciones, bajo mundo, conquista, navegación, barcos | ✅ completada (2026-08-28) |
| **F5** | Akumas, Haki, narradores/auto-narradas, cibernética, tripulaciones | ✅ completada (2026-08-28) |
| **F6** | Cierre y QA: auditoría transversal, seeds finales, OPERACION-STAFF.md, checklist de lanzamiento | ✅ completada (2026-08-28): 67/67 con efectos, 18 paneles A.3, OPERACION-STAFF.md, auditoría transversal |

## 9. Los 67 trámites y las 8 skills

- **Catálogo de 67 trámites** como datos: `inc/ope_rol/tramites/catalogo.php` (número, sistema, nombre, skill, quién, naturaleza, efecto al publicar, plantilla de prompt). Listado cerrado en el **cap. 22.3 del Manual del Staff**.
- **Naturaleza:** `ia` (prompt → IA → editable → firma) · `ligero` (validación + hooks, sin IA) · `staff` (solo el staff inicia) · `hito` (narrativo con firma).
- **Regla de oro:** la automatización nunca decide sola — la IA propone, el staff firma. Únicas **3 excepciones 100 % automáticas**: compra de PP (4) · tirada de fruta (45) · tirada del Conquistador (50).
- **Las 8 skills (Anexo B):** `validacion-personajes` · `cierre-temas` · `creacion-tecnicas` · `mundo-vivo` · `navegacion` · `narracion-automatica` · `adaptacion-akumas` · `adaptacion-cibernetica`. Implementación = plantillas de prompt + flujo editable + firma + documentación staff (no son código de juego).

## 10. Reglas de trabajo

1. **Idioma:** todo en español (código, comentarios, commits, docs, mensajes).
2. **Lee antes de tocar:** este SUPER AGENTS.md → `docs/sistema/REGISTRO_DECISIONES.md` → los manuales. UI: GUIA-ESTILOS-PHP + DESIGN.
3. **Nunca reescribas el skin:** extiende `docs/themes/ope.css` con scopes `body.ope-pg-<pagina>`; cero `<style>`/`style=""` estáticos; botones rectangulares `border-radius:8px` (prohibido pill); la portada usa `body.ope-index`.
4. **Verificación obligatoria al terminar cada tarea:** `php scripts/check-inline-styles.php` limpio · `php scripts/sync-theme.php import` · `php scripts/sync-theme.php verify` → `OK CSS: in sync` · comparación visual en navegador.
5. **BD:** migraciones y seeds en `scripts/` (idempotentes, `MYBB_DB_NAME=rpg_forum`); nunca SQL a mano; respeta backups.
6. **Sin azar** (ver principio 1): si algo del repo viejo tira dados, elimínalo o sustitúyelo por veredicto.
7. **No-crunch** (ver principio 6).
8. **Números sagrados** (ver principio 8).
9. **La automatización nunca decide sola** (ver §9).
9b. **La IA es invisible para el jugador (D6.1):** el front y el `Manual_del_Jugador.md` jamás mencionan IA/skill/prompt — el trabajo se atribuye al «staff»/«equipo de moderación»/«el foro». La jerga operativa (prompt/skill/IA) vive solo en documentos internos (Manual del Staff, OPERACION-STAFF, zona staff).
10. **Commits por fase/hito** en español, sin tocar archivos ajenos; no despliegues manualmente; sin secretos en commits.
11. **Originalidad** (ver principio 7).
12. **No toques** `.freebuff/`, `docs/Prototypes/` ni el flujo de despliegue.
13. **Protocolo guiado (§5):** pregunta con opciones, anota todo en el REGISTRO, confirma cada fase antes de avanzar.
14. **graphify:** para preguntas sobre código, `py -m graphify query "<pregunta>"` (existe `graphify-out/graph.json`); tras modificar código, `py -m graphify update .`.
15. **Herramientas (Windows/PowerShell):** separar comandos con `;`, no `&&`.

## 11. Registro de decisiones y pendientes

- Acta completa: **`docs/sistema/REGISTRO_DECISIONES.md`** (decisiones F0: D0.1–D0.6, mapeo BD→Anexo A, incoherencias detectadas).
- **Cierre F0 (2026-08-27):** 99 tablas `mybb_ope_*` migradas (idempotente) · catálogo 67 trámites · motor (ligero/IA/staff/hito) + firma con histórico probados · bot «OPE Eternal» postea (`scripts/test-7seas-motor.php`) · `bandeja.php` y `tramites.php` renderizan · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Cierre F1 (2026-08-27):** puntero `personaje_tabla` (rol/ope) + `ope7_pj_activo()` · seeds de catálogos cerrados (11 razas, 20 raciales, 7 tribus, 18 dominios, 24 ramas, 28 dotes, 13 defectos, 24 rasgos, 15 efectos de técnicas) · módulo `dominio/personajes.php` (secundarios con raciales, techos, coste PP por tramo, validación de ficha) · wizard 8 pasos con balanzas a 0 y carta en vivo · ficha 7 Seas con desglose auditable + sección solo-staff · trámites 1–12 con efectos al publicar + **ciclo con usuario (3 y 13)** · tests `test-7seas-f1*.php` verdes (wizard, dominio, ciclo+efectos) · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Cierre F2 (2026-08-27):** motor de combate puro `sistemas/combate.php` (PA `6+AGI/10+Nv/5`, daño CC/distancia/desarmado, bandas ±5/±10/±20, Tablas 1–3 + choque + umbral del dolor, matices, estados I–III, 1vN, tope de sala 5, resolución de cierre) · seeds (24 acciones · 34 estados · 10 matices) · **trámites 13 y 62** (técnica → `ope_tecnicas` + cupo INT/4 + PP por tier; muerte → reliquia + herencia reclamable desde el wizard + fruta renace) · **Zona B** (`sistemas/combate_ui.php`: panel bajo el editor, avisos que nunca bloquean, parse + persistencia en `turnos_combate`/`sala_combate`) · panel staff `resolucion-combate.php` (turnos pa_total vs gastado, excesos, veredictos con matices, firma con motivo, histórico de bandas) + enlace en Zona Staff · tests `test-7seas-f2.php` 66/66 + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Cierre F3 (2026-08-27):** calendario on-roll (`sistemas/progresion.php`: semilla = hoy reiniciable, avance perezoso ×2 idempotente en hook, histórico) + finalización de entrenamientos y colocación de reserva (7.3) · **skill-cierre-temas** (Base×7 factores, bandas cerradas, techo/suelo, redondeo a favor del jugador; trámite 2 referencia el veredicto de combate D2.3 y libera congelación) · catálogo `objetos` (34) + `economia_config` · ranuras/carga + equipar con cupos Meitou (trámites 6 y 14) · cartera al crear PJ + movimientos · **tiendas** (15–18: apertura Comerciante/local/capital/bélicos, cierre, reposición, boletín; compra/venta con transacciones y anti-abuso) · `tienda-general.php` y `tiendas-personales.php` funcionales · bloque «Equipo y cartera» en la ficha · **panel staff `calendario-staff.php`** (Anexo A.3: fecha on-roll + avances, presentes activos con congelados, histórico aperturas/cierres, avisos de coherencia; apertura de pasados con fecha declarada, bug F0 del resultado inicial corregido) · tests `test-7seas-f3.php` 41/41 + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Cierre F4.1 (2026-08-27):** **semilla del mundo** (`scripts/seed-7seas-mundo.php`: 7 mares · 17 islas 5.14 con ficha viva de 13 parámetros + histórico de arranque · 34 zonas · 8 tipos de barco ×N1–N3 · 5 maderas · 10 módulos · 7 oráculos · 3 transportes · 8 facciones con 44 rangos) · **motor de ronda** (`sistemas/mundo.php`: abrir/analizar/cerrar, cola de presentes, `ope7_ronda_aplicar_cierre()` — islas con motivo e histórico, recompensas, precios en banda 0,5×–2×, periódico en borrador, visibilidad manual) + helpers (`ope7_isla_por_slug/id`, `ope7_isla_ficha`, `ope7_isla_zonas`, `ope7_isla_actualizar`) · **panel staff `mundo-vivo.php`** (Anexo A.3, scope `ope-pg-mundo-vivo`, card en Zona Staff) · **tienda ↔ isla (D4.4)**: apertura exige isla + territorio + Comerciante, `zona_id` = isla, precio del mercado de su zona, `ope7_tiendas_suspender_en_isla()` para la conquista · `tiendas-personales.php` con selector de isla · tests `test-7seas-f4.php` 36/36 + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Cierre F4.2 (2026-08-27):** **panel staff `progresion-staff.php`** (Anexo A.3, scope `ope-pg-progresion-staff`, card en Zona Staff): cronómetros de entrenamiento por jugador (bloque 5/10, fin + días restantes, al vencer → reserva) · saldos y reservas con progreso de nivel (comprados/10, falta para subir) · **gastos de PP por concepto** (libro `historico_pp`: compras, técnicas, cierres) · histórico reciente (últimos 25 movimientos con fecha/motivo). Libro de PP completado: el efecto 4 (compra) ahora registra el gasto con concepto y tramite_id, y el cierre (2) completa su fila con cantidad/concepto. **Bloque «Reserva de puntos» en la ficha** (F4.2-bis, 7.3): stepper por atributo con techo del nivel, suma live en JS, `gaccion=reserva` → `ope7_pj_colocar_reserva()` con flash. Tests `test-7seas-f4.php` 47/47 + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK. **Cronómetro de dominios** (F4.3, 5.3/4.4 + D4.5): trámite 4 con rama dominios (adquisición con nivel mínimo de PJ + cupo INT 1/50 + multiplicador anclado ×1,5 el 1.º adicional / ×2 el 2.º+ / ×1,0 creación, D4.5; subida nivel a nivel) · cronómetro de 15 días independiente del de atributos con `ope7_pj_finalizar_dominios()` · gasto en `historico_pp` con concepto · bloque de Dominios de la ficha con cronómetro visible y formulario de compra (`gaccion=dominio`) · cronómetros de dominio en el panel «Progresión». Tests `test-7seas-f4.php` 58/58 + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK. **Trámite 38 — Navegación/travesía** (F4.3, 5.16/17): módulo `sistemas/navegacion.php` — validación de ubicación (origen no editable) y un-presente · **límite de mar por barco + madera** (18.5: madera habilita mares, tipo exige madera mínima) · **IRT interno** (base del mar 1–4 + peligrosidad 1–50 + mundo 0–2 − mitigadores Navegante/Timón/Cartógrafo/barco/utensilio; solo-staff) · **oráculos** deterministas por banda (7 tipos, ruta segura en transporte) · **tiempo off-roll** 72/48/36 h por tramo −12 h utensilio −25 % Maestre +incidentes +24 h transporte · **víveres** 1 ración/persona/día on-roll +1/+2/+3, consumidos al cierre con veredicto empeorado si falta stock · cierre integrado en el efecto 2 (raciones + daño al barco + `ubicacion = destino`) · `ope7_travesias_vencidas()` en el cron (plazo del tema). Seed: 4 utensilios (Brújula/Log Pose/Eternal/Log Pose NM). Tests `test-7seas-f4.php` 83/83 + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK. **Panel staff «Navegación»** (A.3, 17.8): página `navegacion-staff.php` (scope `ope-pg-navegacion-staff`, card en Zona Staff) con `ope7_navegacion_panel_html()` — travesías activas por jugador (ruta, medio, plazo, vencimiento con aviso <48 h, oráculos, víveres al cierre) + histórico de resueltas/vencidas. **D4.6:** precio de la ración = 50 ฿ (manual 17.6; seed corregido). Tests `test-7seas-f4.php` 87/87 + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK. **Facciones — trámites 19–24** (F4.3, 5.12/13 + D4.7): módulo `sistemas/facciones.php` — **20 ascenso** sin saltos con requisito duro `rep_min` + cupo del rango (espera de cupo si lleno/sobrecupo) + termómetro · **21 subfacción élite** Shichibukai (cupo 7, revocación → Wanted ×1,5; Gorosei NPC) · **22 cambio** de facción (baja + alta por rango inicial, anti-abuso 1 PJ/facción) · **23 deserción** hostil → criminal (Wanted 5M + infamia) / pacífica → Aventurero libre · **24 infiltración** con capa oculta (`infiltracion_*` en `faccion_personaje`, staff) · histórico inmutable `cambios_faccion`. **Panel staff «Facciones»** (A.3): página `facciones-staff.php` (scope `ope-pg-facciones-staff`, card en Zona Staff) con `ope7_facciones_panel_html()` — tablero de rangos con cupos/ocupación, cúspide, Shichibukai activos, histórico, Wanted. Seed: cupos de cúspide (D4.7) + `rep_min` en requisitos. Bug real corregido en `ope7_faccion_cupo_libre` (sobrecupo = 0 plazas, antes −1 colisionaba con «ilimitado»). Tests `test-7seas-f4.php` 109/109 + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK. **Conquista — trámites 34–37** (F4.3, 5.15/cap. 16): módulo `sistemas/conquista.php` — **34 anuncio** (control previo salvaje/guarnición/jugador, presencia justificada 16.2, anti-abuso de activa duplicada, rondas requeridas 16.3: 0/1/2/3/4+ por fuerza defensiva con fortificaciones +1, suceso público + invitación al defensor) · **35 responder al asedio** (defensa activa → fase asedio + log por ronda; sin defensor no hay veredicto) · **36 resolver/registrar** (duración mínima 16.3, veredicto con motivo, afiliación mixta + quien manda con histórico fuente `conquista`, **suspensión de tiendas del anterior dueño** 16.6 vía `ope7_tiendas_suspender_en_isla`, estado ganada/perdida) · **37 reconquista** (mismas cinco fases, exige conquista previa). **Unidades/hordas (16.7)**: contrato + mantenimiento por ronda, máx 4 por bando, más de 2 exige rango alto (D4.8: cúspide o nv≥30), horda una vez por asedio; crones `ope7_conquista_mantenimientos()` y `ope7_conquista_abandonos()` (16.5: 2 rondas → propuesta, 3.ª → revuelta con motivo). **Panel staff «Conquista»** (A.3): página `conquista-staff.php` (scope `ope-pg-conquista-staff`, card en Zona Staff) con `ope7_conquista_panel_html()` — activas por isla con fases/rondas/ejércitos + histórico con motivo. Migración idempotente (columnas de conquista + `conquista_id`/`bando` en unidades + `conquista_id`/`contratada_por` en hordas). Bug MyBB null→'' en `conquistas.zona_id`/`defensor_id` → SQL crudo. Tests `test-7seas-f4.php` 146/146 + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK. **Barcos — trámites 39–44** (F4.3, 5.17/cap. 18): módulo `sistemas/barcos.php` — **39 compra** (primer barco gratis, precio N1 + madera por clase 18.5 ×0,5/×2/×3/×5 +25 % mano de obra, madera mínima del tipo, acorazado solo con imperio D4.10) · **40 construcción** (Carpintero/Astillero + materiales) · **41 mejora** N1→N2→N3 (diferencia + madera, un paso a la vez) · **42 módulos** (ranuras del tipo/nivel + requisitos de oficio, instalar/quitar) · **43 reparación** (grados leve/moderado/grave con materiales + log `reparaciones`) · **44 venta/desguace** (50 % D4.9, fuera de flota) + `ope7_barco_hundir()` con suceso «naufragio`. Ficha 18.2 (`ope7_barco_crear`) y espacio por raza 18.3 (Tontatta 0/Gigante 5). **Panel staff «Barcos»** (A.3): página `barcos-staff.php` (scope `ope-pg-barcos-staff`, card en Zona Staff) con `ope7_barcos_panel_html()` — flota, catálogo de módulos, reparaciones. **Bug real heredado corregido (navegación y barcos):** `madera_minima` es el nombre de la madera, no un mar — el límite de madera mínima nunca se disparaba. ENUM `barcos.estado` ampliado con `vendido` (migración idempotente). Tests `test-7seas-f4.php` 175/175 + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Pendientes abiertos:** ~~el editor real MyBB aún no inyecta el panel Zona B~~ (**RESUELTO** 2026-08-29: `ope_rol_inject_zonab_editor` en el plugin inyecta `ope7_zonab_editor_html()` bajo el editor en newthread/newreply/editpost vía `pre_output_page`; chequeo `scripts/check-zona-b.php` 15/15 + guía `docs/QA-ZONA-B.md` para el QA visual en XAMPP — pendiente solo el QA visual manual en navegador) · ~~reescritura del plugin viejo a `mybb_ope_*`~~ (**HECHO**, cierre F6.3-código 2026-08-29: plugin y páginas vivas a canónico, 0 tablas legacy activas en BD, restos en código muerto con guard; **limpido de requires (2026-08-29):** `bootstrap.php` cargó por completo los `require_once` muertos de los módulos viejos (`mundo/*`, `tramites/tramites.php`, `sistemas/{enlace,renombre,pl,rachas}` — 22 requires), quedando solo el motor 7 Seas; los callers vivos legacy quedan guardados (index/news → curiosidades, navbar → `ope7_isla_por_slug`, tarea `ope_viajes` → no-op). **D6.6 (2026-08-29):** drop físico del legado — eliminados los 17 stubs `inc/ope_rol_*.php`, los 17 módulos `mundo/*`, `sistemas/{enlace,pl,rachas,renombre}`, `tramites/tramites.php`, `config/viaje_ai.sample.php`, la página huérfana `revision-viaje.php`, la task `inc/tasks/ope_viajes.php` (fila de BD borrada) y 4 scripts muertos; **54 tablas `mybb_rol_retirada_*` DROPEADAS** (0 `mybb_rol_*` en BD); callers vivos migrados a rutas canónicas (`ficha`/`personajes`/`zona-staff` → `core/data|system`, bloque Renombre retirado de la ficha). **D6.7 (2026-08-29):** purga TOTAL del código legacy — eliminados 77 scripts muertos (one-offs de desarrollo que consultaban `rol_*`), 9 funciones legacy del plugin (parsers con hooks comentados: `snapshot_post`, `parse_rpg`, `cu_on_post`, `parse_viaje`, `parse_mision`, `viaje_showthread_end`, `mision_showthread_end`), rama legacy de `ficha.php` (−2.550 líneas), funciones legacy de `core/system.php` (leen `rol_personajes`), 6 funciones muertas de `frutas.php` + 5 de `haki.php`, 15 funciones CRUD de `rol_cards`/`rol_tramites`/`rol_tripulaciones` de `catalogos/gestion.php` (−442 líneas), 3 shims muertos de `catalogos/pj.php`, sección F6.3/F6.4 del esquema y el script `migrate-7seas-f1.php` — **0 refs a `mybb_rol_*` en el código PHP** (verificado con grep de comillas y de palabras en comentarios). **Ya hechos (67/67):** 1–12, 13–14, 15–18, 19–24 facciones (incluye el 19 con efecto real en F6), 25–33 bajo mundo, 34–37 conquista, 38 navegación, 39–44 barcos, 45–47/48–49/50/51 akumas+Haki+despertar+adaptación, 52–55 narradores/auto-narradas, 56–61 cibernética+linajes, 62 muerte, 63–67 tripulaciones ✅.
- **Cierre F5.1 (2026-08-27):** **akumas y Haki** (5.18/5.19, D5.1–D5.4): seed `seed-7seas-akumas.php` (6 fichas canon de 8 bloques + 12 pool T1/T2 + 3 bandas + objetos «Fruta: X» categoria `akuma`) · módulo `sistemas/akumas.php` — **45 tirada** (pool por nivel nv3+/15+/30+, T5 nunca, afinidad −10 % PE, anti-abuso nv7, cupo reservado al asignar y fruta en inventario) · **46 compra** (matriz 150/300/600/1.000/1.500 ×1/×2/×3; familia automática con descuento PP + libro; concepto/concreta a bandeja, la IA propone) · **47 comer** (consume el objeto, asigna `akuma_id`, defectos/dotes con balanza a 0) · **50 Conquistador** (ventanas nv5/15/25/35/45 con 3/8/15/25/40 %, intento registrado, éxito → nivel 1 + histórico + suceso Mundo Vivo en borrador que el staff publica) · **51 subida de Haki** (usos 1/tipo/tema contados al cierre de tema · escalera 200/300/400/500 PP + VOL 55/70/85/95 · despertar automático de Armadura/Mantra a nv10 en el cron · histórico de subidas; BLOQUEADO si no cumple → rechazado) · panel staff `akumas-staff.php` (A.3: catálogo/cupos, pool, histórico, Haki con subidas, publicar sucesos) + bloques «Fruta del diablo» y «Haki» en la ficha + formularios 45–47/50/51 en `paginas.php` (fuente dinámica `akumas`) · migración idempotente (ENUM `objetos.categoria` + `akuma`, `personajes.akuma_afinidad`) · tests `test-7seas-f5.php` 64/64 + batería completa · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Cierre UX (2026-08-27):** hub de trámites del jugador (D4.11–D4.13): **56 páginas `tramite-NN.php`** con formularios generados desde `inc/ope_rol/tramites/paginas.php` · catálogo en **6 áreas** con tarjetas enlazadas y filtros · ligeros implementados (1, 4, 14, 17) al instante y el resto a bandeja (los ligeros sin efecto van a bandeja con marcador `[PENDIENTE]`) · solo-staff con badge sin página · guard del motor por `quien` (el 34 lo inicia el jugador) · paginación a 20 (solicitudes, bandeja, histórico PP) · wizard con narrativa (descripción física, personalidad, historia, notas → columnas `desc_fisica`/`notas` migradas) y bloque «Narrativa» en la ficha · Zona Staff en grid con card Bandeja · tests + `check-inline-styles` + `sync-theme verify` OK.
- **Cierre F5.2 (2026-08-28):** **Narradores y auto-narradas** (5.20/cap. 21, trámites 52–55): módulo `inc/ope_rol/sistemas/misiones.php` — **52 solicitud** (ficha de 6 bloques con condiciones de victoria/fracaso explícitas como requisito duro, requisitos duros 21.2.5, tasa del tablón 5.9, un-presente 5.6; oráculos del acto 1 con el motor de 5.16 — banda del IRT de la isla; abre el tema presente **aventura invadible** y crea el tramo 1 firmado) · **53 posteo de tramo** (anti-abuso: nunca sin posts de la ronda; tramo/acto por duración, oráculo siguiente del plan, aviso de acto final) · **54 apertura de misión** (staff: tablón `en_tablon`, validación dura) · **55 cierre** (staff: acto final + motivo, recompensas berries/PP/fama/objetos con histórico, cierra el tema, suceso de Mundo Vivo en borrador 5.14) · cron `ope7_misiones_ronda_cerrar` (tema cerrado sin cierre → abandonada) · **panel staff `misiones-staff.php`** (A.3, scope `ope-pg-misiones-staff`, card en Zona Staff): tablón CRUD, narradores con cupo 2, auto-narradas en curso, histórico y sucesos en borrador — acceso staff + narradores habilitados · formularios 52/53 en `paginas.php` (fuentes `misiones`/`misiones_curso`) · migración: `tema_id`/`solicitante_id`/`abierta_en`/`oraculos` en `misiones` · **auditoría: bugs reales corregidos** — `invadible` invertido en apertura de tema (presentes no invadibles), `my_post_key` ausente en el form de publicar suceso del panel de akumas, `is_narrador` exigía staff (rol independiente 21.2), `staff_narrador` está en `rol_personajes` no en `rol_cuentas` · tests `test-7seas-misiones.php` 34/34 + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Cierre F5.3 (2026-08-28):** **Tripulaciones** (5.21-ter/cap. 22.9, trámites 63–67): módulo `inc/ope_rol/sistemas/tripulaciones.php` — **63 fundación** (ia, capitán: mínimo 2, ficha nombre/bandera/propósito, barco del capitán con plazas 5.17 y espacio por raza, un PJ por usuario, un-presente 5.6; crea entidad + **cofre común 5.9**, vincula el barco y abre el tema de fundación) · **64 ingreso** (ligero/firma: solo capitán, espacio del barco, PJ aprobado, un PJ por usuario, fecha de ingreso) · **65 baja/expulsión** (ligero/firma: motivo obligatorio, libera plaza y reparte la parte equitativa del cofre al saliente con registro) · **66 cambio de capitán** (staff + firma: cesión o motín con veredicto 5.10/5.14, sucesor miembro activo, roles intercambiados, suceso de ronda en borrador si cambia el nombre) · **67 disolución** (staff + firma: reparto equitativo del cofre con residuo al último capitán, barco al último capitán y desvinculado, estado disuelta) · cron `ope7_tripulaciones_ronda_cerrar` (22.9: 1ª detección <2 activos → aviso con plazo para reclutar; 2ª → disolución automática con motivo) · **panel staff `tripulaciones-staff.php`** (A.3, scope `ope-pg-tripulaciones-staff`, card en Zona Staff): avisos de disolución, fichas con miembros/espacio/cofre, acciones que crean los trámites 66/67, histórico auditable · formularios 63–67 en `paginas.php` (fuentes `mis_tripulaciones`/`miembros_trip`/`pj_sin_tripulacion`) · migración: `aviso_disolucion_en` en `tripulaciones` · tests `test-7seas-tripulaciones.php` 38/38 + batería completa verde (misiones 34/34) · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Cierre F5.4 (2026-08-28):** **Cibernética y Familias Legendarias** (5.22/cap. 23, trámites 56–61): módulo `inc/ope_rol/sistemas/cibernetica.php` — **56 instalación** (ia: zona única, puerta nv10/20/35, **requisitos acumulativos** — suma de todos los implantes, balanza a 0 exacta, vara Cirujano+Ingeniero con autocirugía, pago berries 100k/500k/2,5M + PP 200/400/600; aplica defectos) · **57 retirada** (ligero/firma: motivo, libera cupo y balanza) · **58 mantenimiento** (ligero sin firma: 2.500/10.000/40.000 ฿/ronda, ×2 con «Mantenimiento oneroso», repara averiado) · **59 diseño a medida** (staff: ranura calibrada por la skill, ficha editable) · **60 concesión de linaje** (staff: cupo 3–5, un linaje por PJ, expediente de fidelidad; crea dote + «La sangre llama» −1 si faltan; suceso de ronda) · **61 revocación** (staff: retira dote/defecto, libera cupo, suceso) · cron `ope7_implantes_ronda_mantenimiento` (sin saldo → **averiado**, 23.3) · seed `seed-7seas-cibernetica.php` (3 implantes de la guía §8 **recalibrados a balanza exacta 0** + 6 defectos + 3 familias) · **paneles staff `cibernetica-staff.php`** (A.3, scope `ope-pg-cibernetica-staff`: activos + acciones 56/57/59 + histórico) y **`familias-staff.php`** (A.3, scope `ope-pg-familias-staff`: cupos + acciones 60/61 + portadores), cards en Zona Staff · formularios 56–61 en `paginas.php` (fuentes `implantes`/`mis_implantes`/`familias`/`pj_sin_linaje`/`linajes_activos`); los 59/60/61 solo-staff se inician desde los paneles · fix de robustez: `$mybb` en el global de los paneles · tests `test-7seas-cibernetica.php` 31/31 + batería completa verde (f5 64/64 · misiones 34/34 · tripulaciones 38/38) · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Auditoría de la ficha (2026-08-28):** bloques F5 verificados contra el DDL — **Fruta** (campos/lógica `sin_comer`/afinidad/influencia correctos; el 47 no cambia `estado`, el cupo persiste) · **Haki** (tipos/nivel/usos/PP; conteo al cierre alimenta la columna) · **Tripulación AÑADIDO** (antes no existía: nombre, miembros 👑, propósito, cofre 5.9) · **Misión en curso AÑADIDO** (antes no existía: nombre, tramo/acto, condiciones de victoria/fracaso, aviso 53) · **desglose auditable intacto** (base+racial, secundarios, dotes/defectos con origen — el narrativo no confunde la balanza de creación, dominios, técnicas, solo-staff, sin inline) · test `test-7seas-ficha.php` 22/22 + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Cierre F5.5 (2026-08-28):** **Despertar (48) y adaptación de fruta bajo demanda (49)** — `ope7_efecto_despertar_akuma` (ia+firma: banda de nivel por tier/familia T1–T2 nv25 · T3–T4 nv32 · Logia/mitológica nv40 + no repetido en `despertares`; reporta antigüedad on-roll/temas/VOL; registra + suceso de Mundo Vivo en borrador, la Logia es periódico) · `ope7_efecto_adaptar_akuma` (staff: alta en `akumas` con raw SQL por el ENUM rareza + objeto «Fruta: X»; fruto único) · panel Akumas ampliado: «Sucesos en borrador» cubre haki+akuma, formulario «Fruta bajo demanda» (crea el 49) y sección «Despertares» · prompts 48/49 en el catálogo · tests `test-7seas-f5.php` 80/80 + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Cierre F6.1 (2026-08-28):** **Bajo mundo (trámites 25–33, 5.13) — último bloque de trámites** · módulo `inc/ope_rol/sistemas/bajomundo.php` — **25 solicitar rumor a la red** (ia+firma: capacidad del espía por tipo Novato→Supremo limita categoría/alcance, cobra mantenimiento, la IA propone la ficha de 5 campos y el staff fija veracidad) · **26 comprar rumor** (ligero: multiplicadores fiabilidad ×0,6/×1,0/×1,5 × frescura ×1,2/×1,0/×0,5 con techo global 0,5×–2×, ficha transferida) · **27 contrastar** (ia+firma: coste alcance × sensibilidad ×1–×10, afina fiabilidad un grado y en Sólido revela veracidad) · **28 vender** (ligero: transferencia + copia del vendedor, fiabilidad publicada) · **29 montar/ampliar red** (ligero: contrato + mantenimiento, límite 4 espías, sin saldo bloquea) · **30 publicar cartel** (staff+firma: cifra escala 5.9, paradero con fiabilidad, caducidad a 3 rondas, `recompensas_historico`) · **31 cobrar recompensa** (ia+firma: entrega verificada 5.10, anti-abuso autocaza, paradero frío bloquea, cobro + histórico) · **32 rumor falso** (ia+firma: veracidad interna falsa, fiabilidad publicada del staff) · **33 ataque a red** (ia+firma: veredicto sin dados, espías descubiertos/red baja) · cron `ope7_bajomundo_cron` (14.6: 3 rondas → paradero frío) · **panel staff `bajomundo-staff.php`** (A.3, scope `ope-pg-bajomundo-staff`, card en Zona Staff): rumores por isla con veracidad solo-staff, redes y espías, carteles, histórico de operaciones + acción que crea el 30 · formularios 25–33 en `paginas.php` (fuentes dyn `mis_espias`/`rumores_comprables`/`mis_rumores`/`pj_otros`/`carteles_cobrables`/`redes_ajenas`) · prompts 25/27/30–33 en el catálogo · tests `test-7seas-bajomundo.php` **47/47** + batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Cierre F6.2 (2026-08-28):** **auditoría transversal de cierre (checklist §2)** · **trámite 19 (Reclutamiento de NPC, 12.5)** era el único sin efecto (`[PENDIENTE]`): módulo `inc/ope_rol/sistemas/npc.php` — busca la ficha existente en bestiario por nombre (usa fichas existentes, sin combate), marca «reclutado» en `npc_apariciones` (manejado_por = reclutador, JSON `{}` por el bug MyBB null→'') + case 19 en motor + require en bootstrap · **efectos de mundo del trámite 62 aplicados de verdad** (D2.2 cerrado): cartel retirado (estado `retirado` si wanted_base > 0), baja de facción con histórico (`ope7_faccion_registrar`), suceso de ronda `muerte` en `sucesos` (ronda activa), además de la fruta renacida que ya existía · **3 paneles A.3 que faltaban**: `mercado-staff.php` (fluctuación por zona/ronda con motivo + carteras + transacciones, `ope7_mercado_panel_html` en tiendas.php) · `npc-staff.php` (primarios con capa oculta solo-staff + bestiario + apariciones incl. reclutado, `ope7_npc_panel_html` en npc.php) · `reliquias-staff.php` (fichas muertas con leyenda + histórico con calidad/herencia, `ope7_reliquias_panel_html` en muertes.php, registrado en bootstrap) · cards en Zona Staff + scopes CSS `ope-pg-mercado-staff`/`ope-pg-npc-staff`/`ope-pg-reliquias-staff` · **`docs/OPERACION-STAFF.md` escrito** (guía operativa diaria: bandeja, ronda, panel por panel, skills, números sagrados) · tests `test-7seas-bajomundo.php` **60/60** (añade [11] trámite 19 con bestiario ad hoc + [12] los 3 paneles nuevos, idempotente en 2 corridas) · batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Auditoría de paneles staff vs A.3 (2026-08-28):** **Mundo Vivo** AÑADE «Recompensas con motivo» (`recompensas_historico`) y «Periódico News Coo» (`historico_periodicos` + publicar borrador con `verify_post_check`) · **Facciones** AÑADE «Sueldos y nóminas» (`sueldos`, vista honesta hasta que el cron genere filas) · **Akumas** AÑADE «Ficha de 8 bloques» (`<details>` por fruta con los 8 bloques del JSON real) · **Cibernética** AÑADE «Mantenimiento por ronda» (coste ×2 oneroso, último pago del histórico, situación derivada averiado/pendiente/al día) · texto de narradores corregido (`rol_personajes`, 21.2) · CSS: `.zs-tab` en facciones, `.zs-ficha`/`.zs-bloque` en akumas · batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Anexo B del Staff actualizado (2026-08-28):** 8 skills completas con trámites reales del catálogo y guías maestras — `validacion-personajes` (3) · `cierre-temas` (2/20/60/62/55) · `creacion-tecnicas` (13) · `mundo-vivo` (ronda/mercado/rumores/hordas) · `navegacion` (38) · `narracion-automatica` (52–55) · `adaptacion-akumas` (45/46/48/49) · `adaptacion-cibernetica` (56/59) · índice del manual corregido (✅ completo) · incoherencia del REGISTRO marcada RESUELTA · pendiente eliminado.
- **Cierre D1.8 (2026-08-28):** `ope_temas.tid` ↔ **thread MyBB real** — columna `ope_temas.mybb_tid` (idempotente) · **hook de posteo** `ope_rol_after_thread` vincula el hilo real al presente abierto del autor sin hilo (`ope7_tema_vincular_mybb_por_pj`, salta Off Topic, nunca pisa) · efecto 1 acepta `mybb_tid` explícito (formulario/resultado) · **cierre**: `ope7_tema_cerrar_mybb` marca `threads.closed=1` en los 3 cierres (efecto 2, misión 55, travesías vencidas) · helpers `ope7_tema_mybb_tid/vincular_mybb/vincular_mybb_por_pj/cerrar_mybb` en motor.php · test f1-tramites ampliado (777 no pisa + cierre real) + smoke end-to-end · batería completa verde · `check-inline-styles` limpio · `sync-theme verify` OK.
- **Auditoría cap. 22 vs catálogo real (2026-08-28):** revisado el cap. 22.3–22.6 del Manual del Staff contra `inc/ope_rol/tramites/catalogo.php` — corregidas las filas 19 (ligero/firma con efecto real «reclutado»), 34/37 (staff con IA general iniciadas por el jugador, F6.2), 36 (skill solo `mundo-vivo`), 45 (100 % automático), 46 (matriz 150–1.500 ×1/×2/×3 + concepto a bandeja), 49/59 (naturaleza `staff` no «IA + firma»), 55 (skill `narracion-automatica` con prompt que suma `cierre-temas`), 64/65 (quien solo `capitan`) · listas de naturaleza 22.4 y staff-only 22.5 reescritas contra el catálogo (el 18 es staff-only, no ligero; el 42 es IA, no simple; ligeros añadidos 19/35/57/58) · 22.6 ampliado de ~9 a 30+ plantillas específicas reales (bajo mundo 25–33, conquista 34/37, barcos 40–43, despertar 48, Haki 51, misiones 52–55, cibernética 56–59, linajes 60/61) · **bug real corregido en el catálogo**: `case 49` duplicado en `ope7_prompt_especifica` (el segundo era código muerto inalcanzable) · verificación programática: 67/67 trámites, 8 skills del Anexo B todas usadas, ninguna desconocida · `test-7seas-motor` verde.
- **Regla 9b blindada con test (2026-08-28, D6.1-ter):** nuevo `scripts/test-7seas-regla9b.php` — falla si algún texto **visible al jugador** menciona IA/skill/prompt. Barré páginas públicas + ficha 7 Seas + Manual del Jugador + strings de retorno del motor/efectos + la vista `ope7_tramites_jugador_html` (excluye legítimamente zona staff y comentarios). **Violación real corregida**: el hub del jugador mostraba «ventanillas · motor 5.21» → «ventanillas del foro». Verificado: con violación inyectada falla con la línea exacta (exit 1), revertida pasa 4/4 (exit 0). **Cierre (2026-08-28, D6.1-cuater):** añadido patrón `\bia\b` (IA como palabra independiente, p. ej. «staff/IA») y excluido del barrido [1] las páginas legacy de staff blindadas en bloque (`revision-viaje`, `resolucion-combate`, `mundo-vivo` — no siguen el patrón de nombre `*-staff` pero no llegan al jugador). **2 fugas reales nuevas detectadas y corregidas en el Manual del Jugador**: «el staff/IA los aplica» (11.6 matices) → «el equipo de moderación lo aplica», y «una IA que analiza» (22.1) → «una persona del equipo de moderación que revisa y firma» · añadida la sección jugable **22.1-bis «Quién revisa tu solicitud»** (flujo de firma del staff sin jerga) · `php -l` limpio · test 4/4 verdes.
- **Auditoría Anexo A.1 vs DDL real (2026-08-28):** cotejadas las 100+ tablas `mybb_ope_*` contra el Anexo A.1 — todas las documentadas existen; **4 correcciones de diseño→DDL**: (1) `personajes.estado` es el ENUM de validación (borrador/revision/aprobado/rechazado), la vida es **`estado_vida`** (activa/muerta); (2) `isla_estado` usa **`fuerza_defensiva_nivel`**, no `fuerza_defensiva`; (3) `cofre_tripulacion` es tabla dedicada (berries/log) con `tripulaciones.cofre_id` como vínculo — no reutiliza el mecanismo de carteras de 5.9; (4) añadidos los espejos del front migrados (F6.4) + `cron_log` (F6.5) a la fila «sin sistema propio». Manual corregido; `check-inline-styles` limpio · `sync-theme verify` OK.
- **Anexo A.2-bis (2026-08-28, actualizado 2026-08-29):** tabla de verificación de hooks y crones reales en el Manual del Staff — 20 `add_hook` activos del plugin (global_start ×2, datahandler de posteo ×6, guards ×2, parses ×2, zonab ×2, postbit/threadreview/forumdisplay/showthread/pre_output ×2 — el inyector Zona B de F2.2 se añadió con el QA 2026-08-29) con su función y disparador, 11 subcrones encadenados desde `ope7_progresion_cron` (calendario, entrenamientos, dominios, travesías, conquista ×2, Haki, misiones, tripulaciones, implantes, bajo mundo) y 4 disparos manuales (firma → `ope7_tramite_aplicar_efectos` con los 67 efectos, ronda abrir/aplicar cierre, bot) — cierra la brecha diseño → código y documenta los hooks legacy comentados en D6.3/F6.4.
- **Aislamiento de tests F1–F4 (2026-08-28, D6.5):** corregida la fragilidad de orden/repetición del suite — **f3 no borraba `ope_temas`** (solo sus participantes), dejando 'presentes abiertos' huérfanos que rompían su re-corrida (cierre/calendario) y el «un-presente abierto» de f4 · añadido borrado de temas en su limpieza previa Y final + wipe `1=1` de `ope_temas`/`ope_temas_participantes` de principio (mismo patrón que f4) → **f3 idempotente ×3 y f4 pasa tras f1/f2/f3** · **bug cwd en `test-7seas-regla9b.php`**: el `glob('*.php')` del bloque [1] resolvía relativo al cwd (al invocarlo desde `scripts/` barria migraciones/tests con jerga interna legítima) → ahora usa ruta absoluta `dirname(__DIR__)` y es independiente del cwd (4/4 desde raíz Y desde scripts) · cadena completa F1→F4 verde ×2 pasadas · `check-inline-styles` limpio · `php -l` limpio. ~~**Limitación conocida (fuera de alcance):** el check F5 «Semilla: 18 frutas» es frágil ante una BD de desarrollo con frutas acumuladas de corridas previas (el seed canon es idempotente por `nombre_propio`); para una corrida 100 % reproducible de F5 se parte de una BD recién migrada.~~ (**RESUELTO** 2026-08-29, ver §11-bis: el check ya no exige COUNT total == 18 — valida por nombre las 18 canónicas + ausencia de duplicados).
- **Cierre D6.3 (2026-08-28):** **retirada por capas de `mybb_rol_*`** (decisión consultada: por capas, plugin conservado) · **`mybb_ope_cuentas` creada** como fuente canónica (puntero activo + staff/narrador + slots) con migración idempotente desde `rol_cuentas` · código migrado a la fuente canónica con fallback: `ope7_pj_activo`/`ope7_pj_set_activo` (espejo en `rol_cuentas` para el plugin), `ope7_permisos` (staff/narrador de `ope_cuentas`, sin depender de `ope_rol_active_staff`), `ope7_bot_personaje` (bot en `ope_personajes` uid 2), narradores de misiones y bibliotecas públicas (`ope_rol_cat_*`, leen de `ope_personajes`) · **bug real corregido**: doble prefijo en `ope7_tabla_existe_legacy('rol_cuentas')` → `mybb_rol_rol_cuentas`, la copia de datos nunca corría · **20 tablas archivadas** como `mybb_rol_retirada_*` (solo código muerto, RENAME reversible) · **34 conservadas como legado** (hooks activos del plugin + `gestion.php` vivo) · batería completa verde · permisos/bot/bibliotecas/paneles OK · `check-inline-styles` limpio · `sync-theme verify` OK.

## 11-bis. Batería de tests — orden canónico y precondiciones de BD

> Los tests del motor (`scripts/test-7seas-*.php`) son **autónomos e idempotentes**: cada uno limpia su propio estado al arrancar y al terminar, de modo que pueden correrse repetidos y en secuencia **sin resetear la BD entre fases** (D6.5). Lo que ninguno hace es sembrar los catálogos: **asumen que la BD ya tiene las tablas y los seeds de su fase**. Por eso, sobre una BD recién migrada se ejecuta primero la **preparación** y después la **batería** en orden por fase.

**Preparación (una vez, idempotente) — en este orden:**

| Fase | Migración | Seeds que siembra | Prepara para |
|---|---|---|---|
| base | `migrate-7seas-esquema.php` | todas las tablas `mybb_ope_*` del Anexo A.1 + `cron_log` | todo |
| F1 | (incluido en el esquema) | puntero `personaje_tabla` en `ope_cuentas` | F1 |
| F1 | `seed-7seas-catalogos.php` | razas · raciales · tribus · dominios · especializaciones · dotes · defectos · rasgos · efectos de técnicas | F1 |
| F2 | `seed-7seas-combate.php` | acciones_pa · estados · matices_combate | F2 |
| F3 | `seed-7seas-progresion.php` | objetos · economia_config (+ precios/carteras de arranque) | F3 |
| F4 | `seed-7seas-mundo.php` | mares · 17 islas + `isla_estado` · zonas · tipos_barcos · maderas_casco · modulos_barcos · oraculos_catalogo · transportes · facciones+rangos | F4, Misiones, Tripulaciones, Bajo Mundo |
| F5 | `seed-7seas-akumas.php` | 18 frutas (+ pool T1/T2 y bandas) · objetos «Fruta: X» | F5, Ficha |
| F5.4 | `seed-7seas-cibernetica.php` | 3 implantes · 6 defectos · 3 familias legendarias | Cibernética, Ficha |

**Batería — orden canónico (por fase, aprox. topológico):**

1. `test-7seas-f1.php` (dominio personajes) → 2. `test-7seas-f1-tramites.php` (trámites 1–12) → 3. `test-7seas-f2.php` (combate) → 4. `test-7seas-f3.php` (progresión/economía) → 5. `test-7seas-f4.php` (Mundo Vivo/navegación/facciones/conquista/barcos) → 6. `test-7seas-f5.php` (akumas+Haki) → 7. `test-7seas-ficha.php` (ficha 7 Seas) → 8. `test-7seas-misiones.php` (narradores) → 9. `test-7seas-motor.php` (motor 67 trámites) → 10. `test-7seas-regla9b.php` (IA invisible, sin BD) → 11. `test-7seas-tripulaciones.php` → 12. `test-7seas-cibernetica.php` → 13. `test-7seas-bajomundo.php` (F6).

Se validó (D6.5) que la **cadena F1→F4 corre verde en 2 pasadas seguidas** sin reset entre medias, y que los tests de cada fase pasan aislados con su seed presente. `test-7seas-regla9b.php` es un barrido de archivos (no usa BD) y, tras el fix de D6.5, funciona tanto desde la raíz como desde `scripts/`.

**Notas:**
- Los tests **no siembran**: si arranca una BD fresca, ejecuta primero la preparación de arriba.
- `test-7seas-f1-wizard.php` lanza un subproceso (`_wizard-post-child.php`); su `FALLO: no se guardó` / `FALLO — sin mensaje` son rótulos descriptivos de ramas de control, no fallos de agregación — el exit code 0/1 es la fuente de verdad.
- **Robustez del check F5 (2026-08-29):** el bloque «Semilla» ya **no exige COUNT total == 18**. Valida que las 18 frutas canónicas (6 canon + 12 pool T1/T2, lista fija por `nombre_propio`) están presentes y que **no hay duplicados por nombre** (idempotencia del seed vía upsert) — tolera frutas adicionales acumuladas en una BD de desarrollo (p. ej. creadas por el trámite 49 bajo demanda). Verificado: con 20 frutas en BD (18 + 2 acumuladas de prueba) el check sigue verde y tras limpiar se restaura en 18; F5 con **81 OK / 0 FALLO** en la validación.
- **`scripts/check-parseo.php` (2026-08-29, sin BD):** chequeo estructural de parseo del PHP puro del motor (inc/ope_rol/**, inc/plugins/*.php, inc/tasks/*.php, inc/ope_rol_*.php, scripts/*.php; excluye las páginas híbridas de la raíz y exime `tramites/paginas.php`). Detecta las tres clases de bug que rompieron el plugin: (1) **close tags** `?>` / HTML inline (incluye el `?>`-en-comentario que cierra PHP y que `php -l` no ve), (2) **funciones nombradas anidadas** (solo se definen si el bloque exterior corre) excepto guards `if (!function_exists(...))`, y (3) **tokenizador que no llega al final** del archivo. Validado contra el plugin roto real de git (`5fcfcf0`, 3 reglas disparadas) + smoke 3/3 (close temprano → exit 1, anidada → exit 1, guard → exit 0); **218 archivos / 1018 funciones OK**. Se suma a la verificación del cierre junto a `check-zona-b`, `check-inline-styles` y `sync-theme verify`.

## 12. Definición de done

Checklist completo en el §2 (arriba). Hitos verificables por fase en el maestro (`§11`) y en la tabla del §8.

---
*Documento vivo — se actualiza al cierre de cada fase y de cada sesión.*
